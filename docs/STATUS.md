# Peach 当前状态

最后核验：2026-09-01

本文件只保存当前运行态、已核验能力、仍需完成的工作和自动统计。已完成批次、旧测试数字与事故过程由 Git 历史保存，不在这里累积。

## 运行态

- Windows 是当前 ledger writer。启动入口为 `C:\Users\longm\Desktop\peach\peach-app\dist\Peach\Peach.exe`，代码、数据、worktree 和共享传输点都在 `C:\Users\longm\Desktop\peach\` 下；外置盘只提供 `R:\media`。
- 托盘必须以普通权限启动：提升权限后的令牌看不到 CloudDrive 的 `A:` / `B:`，会把 PikPak 和 115 误报为脱盘。异常时从托盘退出，再由资源管理器或普通权限终端启动。
- Windows HTTP 为 `0.0.0.0:80`，HTTPS 为当前 LAN IPv4 的 443，mDNS 名为 `peach-win.local`。线上服务版本最后核验为 `0.7.13`、`ledger_sync=writer`。
- macOS 是 reader，代码在 `~/Desktop/lmd.gg/peach/peach-app`，数据在相邻 `peach-data`；`peach.local` 经 8900/8443 和 pf 提供 80/443。GET 正常，写入端点返回 409。
- 2026-08-27 核对 macOS：服务子进程已跑当前 `master`，但菜单栏进程仍是旧代码。托盘层改动要生效，需手动退出菜单栏项后由 `.app` 或 `launchctl kickstart -k` 重启一次。
- 两端固定使用不同 mDNS 名和各自本机 CA；CA 私钥、服务器私钥和凭据不跨机同步。生成资产由 Syncthing 单向 Windows → Mac，Git 与 ledger 复制是另外两条链路。
- Windows 真实 ledger 为 `peach-data/database/ledger.db`，已应用到 `0023`，共 24 个迁移且 0 待处理。2026-09-01 迁移前备份为 `database/ledger.pre-migrate-20260901-012434.db`，49 条 JAV 媒体改名前备份为 `database/ledger.pre-jav-filename-normalize-20260901-012519-000799.db`。
- Mac ledger 已于 2026-08-27 经授权从共享副本显式拉取并恢复 `in-sync`，writer 仍是 Windows。拉取前备份为 `database/ledger.pre-pull-gen32-20260827T062519Z.db`；前后 asset 81554、entity 8182、asset_entity 155541，完整性正常且外键违规为 0，证明原 conflict 仅是 SQLite 物理布局与 marker 漂移。
- Mac 的 `peach-data/sources` 已迁到内置盘；`archive`、`tools` 仍可指向外置盘。追更证据和浏览记录不再依赖外置盘。
- 服务运行期不再连接 Stash：2026-09-01 关掉 `StashAdapter` 与整层 backend 契约，FastAPI 的媒体解析只有 `FilesystemBackend` 一条路径（ADR-0021）。Stash 进程是否还在 `127.0.0.1:9999` 上跑与 Peach 无关。仅剩 `scripts/ledger.py stash` 与 `scripts/import_stash_entities.py` 两个离线导入脚本在需要时才连它。Windows FFmpeg/ffprobe 位于 `peach-data/tools/ffmpeg`，macOS 走 PATH。

## 已核验能力

- 2026-09-02 数据管理页：一次刷新只放一段骨架，每张卡多给一行分项，操作条按 Geist 的窄屏做法收敛。**两段动画**的根因是加载态写了两份——深链启动时 `renderInitialSurfaceLoading` 铺一张通用大布局骨架，路由到位后各页再铺自己那张（数据管理那张还是 Loading Dots）。现在 `MANAGEMENT_PLACEHOLDERS` 是唯一定义，两处都从这里取；`skeletonHtml` 给每张骨架盖一个 `data-skeleton` 身份键，`showManagementBody` 认出屏幕上已经是同一张就不再重画——`innerHTML` 换新节点会把 shimmer 从头放一遍，用户看到的就是同一段动画闪两次。浏览器实测：把开机那张骨架摆回 `#stats` 再走一次路由，新建骨架节点 0 个。顺带修好 `/follow-manage` 深链启动时误用关注骨架。**卡片内容**：垃圾文件按类型分项、回收站补占用体积、复核列前三类，数字全部来自同一份 payload，不为第二行多发请求；`q_items` 把条数和体积放进同一次聚合（新增 `bytes`）。重复文件为 0 时写「没有重复内容」而不是 0 组。三个「· 在线」徽章换成一行来源名——卡片要说的是这次扫哪几个来源。**高度**：删掉 `.cleanupfieldset{min-height:176px}`，那条地板是三列时用来对齐同一行的，单列下只是留白；现在无分项的卡 153px，和「网盘与账本」一致，有分项的 174px。**窄屏操作条**按 375px 实测的 Geist 行为重做（证据登记在 `vercel-geist-fieldset-scroller-empty-state` 的 2026-09-02 追加块）：Geist 在 375px 下操作条仍是一行 `row`／`nowrap`，`min-height:56px` 按说明行数长到 65/85/105px，12 颗按钮宽 70–186px 没有一颗铺满。我们此前 640px 以下把条子竖过来、按钮 `width:100%`，那是自己加的；改成固定高度换 `min-height` + 上下 8px，说明 `min-width:0` 可换行、按钮 `flex:none` 不被压。**按钮底色**按用户指定统一填 `--surface`：条子是 `--overlay-5`，更深的底才分得出来，此前数据管理六颗透明底与同页 `.resourceaction` 是两种按钮。Windows 正式全量 1690 项通过、13 项按平台跳过；渲染在隔离预览实例（127.0.0.1:8900，账本副本、只读）按 420px 与桌面宽两档核对。本轮只改静态 JS／CSS／HTML 与一个只读查询，未新增依赖，未写真实 ledger、候选或媒体，未重启或替换生产入口。
- 2026-09-02 多版本第二轮收口四处「看着不像一套」。版次队列标题栏原来写「版本 · ABF-216 · 2 个版本」，番号正上方的详情标题已经印着，h2 也写着「版本」，`editions` 队列的副题只留「2 个版本」（别的队列标题带播放列表名、Mix 种子，不砍）。队列行没有头像、和「接着看」并排像两套控件：把卡片的身份推导抽成 `cardIdentity(it,linked)`，卡片与队列共用一份，队列传 `linked=false` 出 `<span>`——整行是一个 `<button>`，再嵌 button 会被浏览器就地拆散。「接着看」216px 窄卡的高度不齐：第三个头像挤掉署名行宽度，元数据折成三四行；现在窄卡只显示两个头像、元数据钉成一行不换行、大小与观看次数不放、推荐理由截尾——第一版用固定两行高加 overflow 裁，行高凑不出整行，「399 MB」露了半截，已改掉。深链冷启动（`/editions/86286/86286`、`/item/N`）详情下方那张「正在读取作品」骨架永远不消失：列表一次请求都没发过，`openItem` 在没有返回面时直接清掉（`clearIdleCatalogLoading`），关详情由既有 `detailReturnNeedsRestore` 补装。顺手修播放器：播放键 hover 高亮层是 40×32 胶囊、不是圆，圆钮的高亮统一成内缩 4px 的同心圆（音量键、时间钮原本就是），左上统计钮、右上关闭钮同样处理；音量键、统计钮、关闭钮的底色 .3 和播放键的 .6 不是一档，统一 .6；video.js 自带 Arial、加载速度徽章与统计面板写死 Cascadia Mono，播放器内文字统一跟页面主字体。托盘已重启（pid 46072）。
- 2026-09-02 多版本合并补上另外三个表面。折叠逻辑本来只在首页与搜索的 `batchWithMix` 里；资料页网格单独调 `collapseMultipartItems`，版次组照旧两张卡，而两张卡上都挂着「2 个版本」角标——角标说合过了，眼前是没合的两张（女优页 `释爱丽丝` 实测 18 → 13 张）。版次队列的点击没有 `editions` 分支，掉进播放列表分支带 `undefined` 的 `playlistId` 发请求，表现就是点了没反应；标题也落到「视频合集」兜底，每条不写自己是哪个版次。现在队列标题为「版本 · <番号> · N 个版本」，每条标题同一行前置 `有码`／`无码`／`中字`／`无码破解` 徽章（`有码` 是队列专有：卡片上不加角标即正片是对的，队列里两条对照时空着等于不说）。`/editions/{seed}/{item}` 此前没进 `api.py` 的 SPA 路径表，前端路由写好了也打不开，直接访问是 404。队列缩略图另加封套回退：版次组里常有一份刚入库、`has_thumb` 仍为 false 的无码，只认抽帧会让它在队列里是纯黑块，而同一条在列表卡上是有封面的。Windows 正式全量 1689 项通过、13 项按平台跳过；托盘已重启，生产 HTTPS 实测 `/editions/86286/86286` 返回 200、点第二条切到 `/editions/86286/86285`。本轮只改静态 JS／CSS 与一条 SPA 路径登记，未写真实 ledger、候选或媒体。
- 2026-09-02 关注页四处「同一件事两套口径」一并收口，在线资产就地播放，人脸检出换成 YuNet。**角标与队列**：paheal 一组 9 条里有 1 张图，卡上写「9 个版本」、播放角标写「8 个视频」，点开也是 8 条；`2B Camp [4K]` 更极端——卡上写「2 个版本」，同组另一条不是可播视频，`collection` 因此整个为 null，点开只有 1 条。两处改为共用 `followOpenableItems(group)`，`count > 1` 才出角标。**WIP 贴错条目**：`has_wip` 是组属性（`any(... for item in self.variants)`），角标却挂在主条目标题旁边，`2B Camp [4K]` 判的是 alt 却被读成半成品；现在这一条本身是 WIP 才写 `WIP`，同组有则写「含 WIP」。**`[1080]` 与 `[4K]` 分不到一组**：`_ALT_MARKERS` 只认 `(\d{3,4})p`，括号里写的是不带 `p` 的裸数字，于是 `2b camp 1080` 和 `2b camp` 是两个键；补了已知分辨率档位白名单，`Sayuri - Cowgirl 2`、`Scene [2023]`、`Part [1200]` 都不受影响。**paheal 上传时间被抹**：每个候选都要单独打一次详情页，被限流时 `_detail` 返回 {}，而 upsert 无条件覆盖，把上一轮取到的时间和时长写成 NULL，`COALESCE(published_at, first_seen_at)` 于是让界面显示抓取时刻——实测 168 条里 7 条如此；详情页加退避重试，upsert 改成 `COALESCE(excluded, 现有)`。**在线资产**此前在馆藏详情里只给一道闸；`path` 是来源作品页不假，但能播的代理一直都在（`/follow-stream?id=<follow_item>`），保存时写的就是 `follow_item.asset_id`，`/api/item` 反查回传 `follow_item_id`，播放器直接用，反查不到才拦。同时补上一直没有的「下载到本地」：关注详情一个 `<a download>` 走 `download=1`，服务端按条目标题生成文件名（RFC 5987，中日文落得下来），不回传上游地址。**人脸检出**：`pyproject.toml` 钉的是 `opencv-python-headless==5.0.0.93`，而 OpenCV 5 把 Haar 级联移出了 Python wheel（`cv2.CascadeClassifier` 不存在、`cv2/data/` 只剩 `__init__.py`），两个取景脚本一直抛 AttributeError，954 张封面 0 个 sidecar——「人脸取景没生效」的成因在这里，不在取景公式上，`docs/REUSE.md` 那条也一直和实际装的版本对不上。新增 `peach.face_detect` 两脚本共用，改用同属 OpenCV 的 `cv2.FaceDetectorYN`（YuNet 不是 OpenCV 的替代品，换的只是检出器这一层），模型按固定 URL 取一次并校验 sha256（`8f2383e4…52fa4`，232,589 字节）落 `peach-data/tools/yunet/`、不进 Git，校验不过的文件直接删掉。实测 12 张封面检出 11（Haar 是 46 张检出 24、512 张头像检出 313），`259LUXU-1490` 检出 `cx=0.813` 即两栏封套右侧那个人，`ABW-232` 检出 10 张脸、按面积最大的正落在右侧正封；sidecar 已写 `cx`，契约层 `cover_frame` 目前仍只下发 `cy`，横向取景留作下一步。Windows 正式全量：关注批 1658 项、YuNet 批 1664 项，均 13 项按平台跳过。**rule34.xxx 标签类型回填进行中**：2153 条里已补到 241 条，可反复运行、随时接着跑。三轮实测钉出频率口径——0.35 秒间隔 400 条只取到 28 条，1.1 秒 120 条取到 51 条，加上详情页退避重试后 100 条取到 89 条；那 372 条「帖子页没有解析出类型」是假的，事后逐条直取全部 200 且 `#tag-sidebar` 正常，也就是「被限流」和「确实没有类型」在数据里长得一模一样，现已在连接器里退避重试三次、仍取不到才留空交给下一轮。回填脚本的备份一并从 `shutil.copy2` 换成 SQLite 备份 API：账本是 WAL 模式，已提交但未 checkpoint 的事务在 `-wal` 里，只复制主库会得到一份少了最近改动、看起来却完全正常的账本。本轮未替换生产入口，未删除媒体，`follow_item` 条数前后一致（3582 → 3582）。
- 2026-09-02 管理区归并到「数据管理」，界面口径说明整批清退，三处交互缺陷已修。人工复核、回收站、高清版和垃圾文件、重复文件、空文件夹是同一件事的不同步骤，此前散在管理菜单里各占一行；统计页还挂着链接管理与资源同步两块面板——它们改的是账本和外部现实的对齐，跟「库里现在有多少」不是一件事。现在都归到数据管理一页（原「数据清理」，路由 `/data-cleanup` 不变），管理菜单只留统计／口味／数据管理／关注；`MANAGE_SECTIONS` 仍是全量身份注册表，直达 URL 和「钉到顶层侧栏」都不受影响。新增 `/api/review?counts=1` 只回计数：卡片要的是一个数字，完整 payload 带着每条候选全文是兆级。**文案判据**（已写回 `peach-web-ui`）：界面不解释数字是怎么算出来的，口径、免责、隐私声明、「不会做什么」「按什么汇总」一律删，单位跟着数字走（`待同步 497 项`）；上一轮跑完的扫描结果不常态显示——那是那一刻的快照，而页面上没有任何东西说它是旧的，因此只有仍在跑的扫描才自动接管页面。**框体**按用户指出的 Geist 规范重做了两轮：第一版给标题加了独立横条和下边框，实际 `vercel.com/geist/fieldset` 的 Multiple Fieldsets 示例全框只有一条线（在底部操作条上方），标题是正文区第一行；现为 `fieldsetTitle()` + `--fieldset-bar-h:52px` 的操作条，按钮一律靠右，左侧有说明时说明推到最左。原生 `<legend>` 不再使用：它在上边框上开缺口，同组卡片内容高度不同时缺口位置也跟着不齐。链接统计不再挤成一行——`官网 · 事务所` 里的间隔点和拼接用的间隔点撞在一起，读出来是「社媒 373 · 官网 · 事务所 224」，分不清哪个数字属于哪一类；类型名改成 `官网/事务所`，每类各占一格，「最多的站点」单独一行。**三个缺陷**：直接打开 `/parts/28125/28125` 再关闭播放器，首页永远停在加载骨架上——判据原先是「`#grid` 有没有子节点」，而深链启动时网格里正躺着那张骨架，它也是子节点；卡片一定带 `data-id`，骨架没有（浏览器实测复现并验证）。撤销原先是「关掉回执 + 另发一条已撤销」，底部对齐的栈里一进一出会整块跳一格，改成结果写回同一条 toast，退场也先把高度写死再过渡到 0。设置面板给 body 加 `overflow:hidden`，滚动条一消失整页横向跳一次，`html` 预留 `scrollbar-gutter:stable`（实测位移 0）。**关注详情标签**按 rule34.xxx 自己的分类顺序分组：2026-09-01 实测帖子页 `18622796`／`18622794`，`#tag-sidebar` 的 `li.tag-type-*` 出现顺序均为 copyright → character → artist → general → metadata，组内按名升序，缺的类型跳过不占位，证据与哈希已登记进 `rule34-follow-tags-and-collections`。**合并分卷与版本**成为设置项（浏览组，默认开）；折叠逻辑本来就在，缺的只是开关，实测关掉后首页卡片 77 → 78。Windows 正式全量通过、13 项按平台跳过；数据管理、统计与设置面板在隔离预览实例（127.0.0.1:8900，账本副本、只读）核对渲染。本轮只改静态 JS／CSS／HTML 与一个只读 API 分支，未新增依赖，未写真实 ledger、候选或媒体，未重启或替换生产入口。
- 2026-09-01 `/api/related` 从 6.2 秒降到 0.48 秒：`rank_related` 的 MMR 每选中一条，就把整个候选池和**全部**已选重算一遍 closeness，代价是 O(limit² × 池)；候选池上限 4000，生产实测 limit=20 要 3.4 秒、28 要 6.3 秒、60 要 32.8 秒，而单张 poster 只要 4.8 毫秒——耗时几乎全在排序，不在 SQL。「候选对已选集的最大 closeness」改为增量维护（新选中一条时只和这一条比一次、取较大值），并把 `_sets` 的重复构造提到循环外；代价降到 O(limit × 池)，同一台机器对比 limit=28 为 6.29 → 0.48 秒、limit=60 为 32.75 → 1.10 秒。这是等价变换，不是调参：同一账本上 6 个 seed（2413／485／31025／22887／86187，limit 8／20／28）的 `/api/related` 响应新旧**逐字节相同**；朴素实现作为基准留在 `tests/test_related.py`，等价性对 limit 0／1／3／8／25／200 × diversity 0／0.22／0.6 全部成立，另加一条线性代价上界，任何「每轮重算全部已选」的写法会当场超出。详情页「接着看」、点开 Mix 和悬浮翻动共用这条路径，一起受益。Mix 悬浮翻动跟着从 5 张提到 9 张，第一张改为面板建好后 420 毫秒就翻、不再干等一个完整间隔：实测鼠标停下到第一次翻动 942 毫秒（上一版 1801 毫秒，旧 related 下为 3.1–7.6 秒），9 张面板全部解码成功，中途离开能把预热定时器一并收掉，1440 视口横向溢出 0。变基后工作树 Windows 正式全量 1640 项通过、13 项按平台跳过。本轮未写真实 ledger、候选或媒体，也未新增接口、依赖或迁移。
- 2026-09-01 首页 Mix 卡片不再复用同屏第一张封面，并按用户指定加了悬浮封面翻动（这一条动效是用户当场要求，覆盖 `peach-web-ui` 的「无证据不新增动效」默认）。seed 旧写法是 `visible.find(有署名)`，而馆藏里几乎每条都有 creator，那个 `find` 恒等于 `visible[0]`，Mix 卡片又固定插在第 8 位，两者必然同屏；内容一直是对的（队列仍是 seed + `/api/related`），错的只是代表图的选取，因此只改选 seed 这一步：从 Mix 位再往下隔一屏开始找，且要求这一条真能画出图。悬浮时把该 Mix 的前 5 部作品逐张翻走，1.1 秒一张，翻动的每一张与静止封面共用 `mixFacePoster`，门槛沿用悬停预览（多选、遮挡、滞后动画偏好和滚动中都不启动，离开即停）并注册 `_stopHover` 交给 `releaseHoverPreviews` 统一收掉；相关作品按 seed 只取一次，悬浮预取后点开 Mix 不再发第二个请求。浏览器实测揪出两个只看代码看不出的缺陷：翻动的封面插进的是 `hidden` 容器，`loading="lazy"` 没有布局盒就根本不发请求，实测除第一张外四张 `naturalWidth` 全为 0，故翻动一律 `eager`、静止封面仍 lazy；能否画出图的判据必须和渲染器一致，只看 `has_cover||has_thumb` 会把非 JAV 模式下只有官方封套的条目当成有图，选作 seed 或翻到它都是一张「无预览」，现已收敛为共用的 `mixHasPicture`。变基当前主线后 Windows 正式全量 1592 项通过、13 项按平台跳过。开发服务 1440×1000 实测三次刷新：Mix 均在第 8 位、seed 落在 DOM 第 16／19／20 位且与首张卡片不同，5 张面板全部解码成功、`on` 索引 0→1→2→3→4→0 循环，移出后容器清空并 `hidden`，整轮只发一个 `/api/related?id=…&limit=28`，`body.scrollWidth` 1430 ≤ 1440；390×844 为单列 358 px、横向溢出 0，触屏无 hover 且 `openItem` 开头即调 `releaseHoverPreviews`，不留野 interval。截图未取得：内置浏览器在视口模拟下连续返回纯黑画面，上述结论来自 DOM 与 computed style 读取。本轮只改静态 JS／CSS，未新增接口或依赖，未写真实 ledger、候选或媒体，未重启或替换生产入口。
- 2026-09-01 死链分「搬走了」和「没了」，资料页链接按实体类型分工，管理页新增链接管理。**404 复查**：新增 `rediscover_entity_links.py`，从死链所在站点的索引页逐层上溯找锚点，候选必须回 200 **且标题里有这个人的名字**——列表页 `/talent/` 同样回 200，少了这一关四百个人会被改写成同一个链接。84 条 official 404 里只有 5 条真的还在，全是 T-POWERS 的同一种结构变更（`/official/talent/X` → `/talent/X/`）。第一版还把 `上川星空` 的 KRONE 死链「修」成了 `x.com/tenma_yui2`：人是对的（`天馬ゆい` 就在账本别名里），但那不是事务所页面，结果会是一条标着「KRONE(クローネ)」、点开却是 Twitter 的 official 链接——比死链更糟，因为它看起来是对的。已加同站约束（`registrable` 取末两段，`co.jp` 这类二级后缀取三段，`evil-t-powers.co.jp` 不算同站）。**资料页分工**：社媒收成纯图标；官网在厂牌页只显示大写域名、不放图标（那一页的头像就是厂牌 logo，再放同品牌小图标是把一件事说两遍），在女优页保留图标与事务所名（头像是人，不构成重复）。别名去掉「别名 ·」前缀、空则整行不出；馆藏检索词整行删除；数字提到正文色；同台艺人不再显示部数。**外链圆标**：新增 `link_marks.py` 与 `/link-mark`，把单色字形 favicon 做成「品牌色底 + 白色主体」。实测 T-POWERS 带透明且只有一种绿，做得出；HEYZO 是白底多色字形，抠图会反转成一团糟，因此判据收成「有真透明 + 色相簇 ≤ 2」，不满足就原样缩放，取不到就回落地球——半吊子的抠图比原图更难看。端点按**链接 id** 解析地址，绝不接受前端递来的 URL（和 `/follow-stream` 同一条规矩，否则就是一个任意地址抓取的口子），顺带浏览器不再直接向对方站点发请求。**管理页**：`web_links.py` 提供 `/api/links`（纯读库的分布概览）、`/api/links/check`（后台线程 + 可轮询，七百多条要几分钟）、`/api/links/prune`（删除前逐条重验，只接受刚跑完那次的 `check_id`）；面板放在资源同步上方、沿用同一套盒子，结果分成「已失效」和「取不到」两张表，后者不删只重试。表里的地址用 Geist 中间截断——`/official/talent/X` 与 `/talent/X` 的差别就在尾部。
- 2026-09-01 非原生容器的浏览器转码已改为性能优先：缓存未命中时以可取消 ffprobe 探测，H.264 8-bit 视频优先直接复制到 MP4，AAC 同时复制；其他 Windows 视频先走 CUDA/NVDEC + `scale_cuda` + H.264 NVENC `p1`，解码器不支持时保留 NVENC 编码，两条失败才回退原 `libx264 veryfast`，macOS 保持封装复制或软件转码。真实 CloudDrive POC 中，H.264/AAC 的 30 秒 MKV 片段封装耗时 0.64 秒，1080p HEVC 的 30 秒硬件转码耗时 1.60 秒；生产源码对 1 MB、3.8 秒 AVI 的实际策略为 `ffprobe → cuda-nvenc`，含网盘首次读取和进程启动共 5.59 秒，输出 H.264 `yuv420p` + AAC，临时缓存已删除。变基后工作树与集成后主目录 Windows 正式全量均为 1586 项通过、13 项按平台跳过；生产托盘已正常重启并重新拥有 80／443，项目 CA 严格 HTTPS `/healthz` 返回 200、0.7.13、writer、非只读。未替换 EXE，未写真实 ledger、候选或原媒体，旧转码缓存继续有效。
- 2026-09-01 图片灯箱按用户截图再次纠偏：女优／厂牌本地照片和关注在线图片继续复用同一套 Swiper 14.2.0；首次重叠的根因是只等脚本、不等样式就构造轮播，现在 CSS／JS 同时就绪后才初始化，并用 scoped flex 结构保证任一时刻只画一张主图。上一张、下一张和关闭统一为 48 px、黑色 60% 覆盖层圆钮，首尾无效方向完全隐藏；信息、页码和缩放回到底部，缩放条按原图像素显示 10%–400%，补齐「适应窗口」与「原大小」，主图切换时把当前缩略图主动滚到中央。沉浸动作列只共享 48／24 px 几何，保留白色 10% tonal 表面。`catalog` 588 项、`follow` 501 项（1 项按平台跳过）及 Windows 正式全量 1587 项（13 项按平台跳过）通过；未新增依赖、API 或 ledger 字段，未写真实 ledger、候选或媒体。
- 2026-09-01 资料页移动端按 beeg 收口，链接改为写入前必须能打开：窄屏改单列居中、头像在名字上方（左像右文是给宽屏的，手机上 92 px 头像旁只剩两百多像素，别名和链接被挤成两三行）；社媒收成 42×42 纯图标，handle 变成 sr-only 加 `title`（handle 是网址的一部分，写出来只是把 URL 抄一遍）；删掉外链箭头；X 改用内联品牌 SVG——它本来就挡 favicon 请求，资料页上那个空白白圆就是它，而 416 条社媒里 372 条是 x.com／twitter.com。`.sorts .javlayout` 补 `flex:none`：`.sorts` 是不换行的横向滚动条，`.javlayout` 是带 `min-width:0` 的 fieldset，两条合起来允许它被压到内容宽度以下，窄屏上三个 34 px 版式按钮叠在一起并压住「发行时间」；`.sorts button` 早有 `flex:0 0 auto`，但 fieldset 不是 button，选不到它。375 px 实测横向溢出 0、版式组右缘 177 < 「发行时间」左缘 188，桌面 1280 px 仍是两列左对齐。**用户由一条 404 揭出的数据事故**：首批 703 条链接一条都没验就装进了账本，逐条实测 708 条里 152 条打不开（21%），其中 126 条 404 是确证已没了、26 条只是取不到（`linktr.ee` 403 挡爬虫、`facebook.com` 400、`x.com` 500、连接失败与超时各一）。根因是把 minnano-av 的旧快照当成现在的事实——T-POWERS 改过站，`/official/talent/X` 早已 404，正确形态是 `/talent/X/`，而资料页上它和好链接一模一样。可达性门槛因此放进安装器（所有来源进入账本的唯一入口），并新增 `--prune-dead`；「取不到不等于没了」写成 `is_gone` 判据，只有 404／410 才算删除依据。清理 126 条待用户授权。
- 2026-09-01 实体链接从「采了没处安装」变成可安装：`entity_link` 表、`q_entity` 的 `links` 契约和资料页 favicon 渲染 2026-08 就已就绪，缺的只有安装路径，因此 8178 个实体长期只有 5 条链接（3 条来自迁移 `0003` 的种子，2 条来自 Stash 导入）。这是本仓库第四次出现同一形态，前三次是 `creator_tags`、`studio_logos`、`performer_avatars`。新增 `install_entity_links.py`（默认 dry-run，`--apply` 必须同时给 `--backup`，靠 `UNIQUE(entity_id,url)` 幂等，URL 归一后重跑为零写入，实体解析按 id → 规范名 → 别名，非法行照样出现在计划里并写明拒绝原因）。女优侧新增 `harvest_performer_links.py`：minnano-av 检索唯一命中时会跳到 `actressNNN.html`，**只采信最终地址**——检索页正文里同样有一堆 `actressNNN.html`，那是「相关女优」，按正文解析会把别人的社媒安到这个人头上；为此给共享 `HttpResponse` 补了 `url` 字段，此前两个 transport 都开着 `follow_redirects` 却把最终地址丢了。关键是名字链：账本 performer 规范名是简体中文，日文站按它搜 12 位只中 1 位，改用 `entity_alias` 里的日文写法后同样 12 位全中，罗马字不进链。全量 541 位（26 位按非 JAV 跳过）产出 689 条链接、409 位有链接，`social` 400 条、`official` 289 条，事务所 T-POWERS 128、マインズ 48、Bambi 32 等；132 条未取得中 51 条需人工消歧、7 条没有可用于日文站的名字。厂牌侧新增 `harvest_studio_sites.py`（按厂牌名规则化推域名当假设，能否采信取决于取回页面的标题自述厂牌名，另设停放页与空壳页两道拒绝——抢注页同样回 200 且标题里同样有厂牌名，只看标题会把它采信成官网再被下游当作社媒来源），并把 2026-08 人工看图确认的 14 条厂牌 X handle 转成可安装格式，3 条 `未取得` 标记正确排除。资料页外链 favicon 补上 `referrerpolicy="no-referrer"`：锚点的 `rel="noreferrer"` 只管点击跳转，管不到这个 `<img>`，同页 taste 行早已是 no-referrer，链接从 5 条涨到七百多条后这一处漏才真正有代价。经用户授权已写入真实 ledger：写入前备份 `database/ledger.pre-entity-links-20260901.db`，`entity_link` 5 → 708（+703，与计划一致，0 条跳过），`asset` 75499、`entity` 8178、`asset_tag` 81196、`asset_entity` 145922 均未变，`integrity_check=ok`、外键违规 0。服务实时读账本，无需重启：项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、`db=available`，`/api/entity` 实测释爱丽丝由 0 条变 2 条（`New Actor eXperience` + `X @alice_710_`）、凉森玲梦 2 条（`T-POWERS` + `X @remu19971203`）、Glory Quest 1 条。厂牌官网这一路的首轮结论已作废并重做：当时 56 个厂牌只命中 3 个，53 个失败里 49 个是 `PoolTimeout`——客户端连接池耗尽，不是「这些站不存在」。真实成因是这种形态下**失败的连接会漏掉池槽**，池子多大只决定它第几个请求崩（我一度误判成闲置隧道占满，把池调小反而更早崩）；改成每个请求新建 client 并立刻关掉后 `PoolTimeout` 归零。判据同时补了三道，每一道都由实测假阳性推动：标题只回显域名的通用站（`prestige.com` 标题就是 `prestige.com`，而真站是 `prestige-av.com`）；标题自述在出售域名（`kawaii.com - domain for sale` 的关键词在正文第 81683 字节，超出任何窗口，但它写在标题里，此前从不搜标题；`Attackers - The Domain Name Attackers.com is Now For Sale.` 中间插了词）；以及最关键的——「标题含厂牌名」证明不了这是**这个**厂牌的站，`madonna.com` 是歌手、`hunter.com` 是 Hunter Engineering、`bazooka.com` 是车载音响，全都 200、非停放页、标题含厂牌名，缺的那一半是「这一页是不是 AV 厂牌站」。日站普遍把品牌名写成假名（`本中`、`無垢`、`ダスッ`），拉丁名比标题对整类真站必然失败，因此补了「域名由厂牌名推出 + 页面是成人站」这条替代路径，且该判据必须落在**最终**主机上——实测 `Natural High` 的推导域名重定向到了 `linkedin.com/in/fareedkhan`，按请求地址判会把一个 LinkedIn 个人主页确认成厂牌官网。最终 56 个厂牌 12 条 `ok`（Prestige、MOODYZ、Idea Pocket、FALENO、Wanz Factory、Deep's、Tameike Goro、Muku、Hon Naka、Glory Quest、Fitch、HEYZO）、8 条 `weak` 待人工确认、36 条未取得；`s1s1s1.com`、`madonna-av.com` 这类推不出来的域名要靠 `--seeds` 人工补。
- 2026-09-01 在线图片详情复用既有照片灯箱，但不再显示只适用于本地资产的“在资源管理器中显示”：信息面板改为显示安全的来源、合集序号（有多张时）和浏览器实际解码得到的像素尺寸，来源已提供大小时再追加大小；本地图片原有文件信息和定位能力保持不变。真实关注条目 `3559` 的 Rule34.xxx 图片在 1280×720 与 390×844 下均显示 `Rule34.xxx · 3000 × 4000`，本地定位按钮隐藏，页面无横向溢出且控制台错误为 0。Windows `follow` 497 项通过、1 项按平台跳过；改动只有静态 JS／CSS，无需重启生产服务，未写真实 ledger、候选或媒体。
- 2026-09-01 截图所列 20 个误归 `_未知厂牌` 的 JAV 番号已重新核验并归位：ABF／ABP 共 12 个归 `Prestige`，STARS 2 个归 `SOD Create`，259LUXU 6 个按 JavDB 详情页归 `ラグジュTV`。实际移动 27 个文件、更新 26 条 ledger 路径，补齐 7 条原为空的厂牌关系；`259LUXU-1468.mp4` 原本未入 ledger，只移动文件而未擅自新增资产。目标结构固定为 `厂牌/番号/文件`，5 个冗余标题子目录及旧 `_未知厂牌` 空壳均已删除，最终 20 个目标番号目录都有文件且下方子目录为 0。写入前备份为 `database/ledger.pre-studio-rehome-screenshot-20260901-024500.db`，执行计划为 `generated/studio-recheck-plan-screenshot-20260901.csv`；前后 asset 均为 75499，`integrity_check=ok`、外键违规 0、旧路径 0。新增 `scripts/rehome_unknown_jav.py` 只接收人工确认映射，先 dry-run，并拒绝同名冲突、缺失 ledger 行及厂牌冲突；CloudDrive 删除空目录的竞态已有回归测试，Windows `tooling` 147 项通过。本轮没有重启或替换生产入口；截图外另有 58 个 259LUXU 目录仅生成 dry-run，不在本轮真实写入授权范围内。
- 2026-09-01 JAV／FC2 标题、官方标签和详情身份排版收口：官方标记改为常规字重；女优姓名在头像下视觉居中，桌面单元从 62 px 收到 58 px 并把间隔收为 10 px，同宽详情可多放一列，厂牌等非女优身份继续左对齐。r18.dev 的 `Foot Fetish`／历史 `足系` 统一显示为「美腿」，`Shaved Pussy` 纳入「白虎」候选；标题显示链路已核实，未显示的根因是真实 ledger 的 `catalog_title` 为空而候选尚未批准。FC2CMADB 现有 442 条文章证据中取得 265 条标题、189 条标签和 264 个封面 URL，已生成 450 个标题／标签字段候选；FC2 封面专用批次对 440 个馆藏番号取得 130 张可解码封面、310 个未取得，`FC2-PPV-3701252` 为 1200×1361，`FC2-PPV-2113883` 上游无文章记录且仍未取得。Windows 正式全量 1496 项通过、13 项按平台跳过；候选重建和封面批次均只读打开 ledger，未批准候选、未写真实 ledger 或改媒体文件。
- 2026-09-01 JAV 详情身份区收齐到同一内容起点：115 来源图标、`厂牌` 标题、厂牌头像和名称不再逐层缩进；全站链接交互不再使用文字下划线，女优名以颜色、背景和描边反馈。文件名清洁仅在实际移除方括号域名后规整残留分隔符，避免把无关双连字符文件误纳入批次；最终计划 49 条全部完成，失败、源缺失、目标缺失和 ledger 行不一致均为 0。真实 ledger 已先后备份并应用 `0022`、`0023`，asset 75499、asset_tag 81196、entity 8178、asset_entity 145915 均未改变，`integrity_check=ok`、外键违规 0。最新主线 Windows 正式全量 1482 项通过、13 项按平台跳过；生产托盘与 80／443 子服务已用普通权限重启，项目 CA、SNI、主机名严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读。内置浏览器实测桌面详情内容起点为 882 px；390×844 手机下来源、标题、头像和名称内容起点均为 35 px，横向溢出 0；女优链接计算样式为无下划线。
- 2026-09-01 操作回执、关注媒体与 GoFile 归组已完成：详情工具栏、关注卡片、稍后看、回收／垃圾处置、目录核对、播放列表、关注来源、凭据与批量操作统一复用终态 Toast；能安全反向写入的操作提供 8 秒撤销，等待按钮保持可聚焦但变灰且阻止重复提交。关注视频／图片详情复用同一媒体选择路径；显式图片／视频筛选即使为 0 项也保留切换控件。一个 FANBOX 作品仍是一个外层合集，多个 GoFile 链接只在合集内部按文件夹标签分段并保留各自媒体归属。F95 已保存会话的下次显式检查会重解析旧条目，不再被 304 短路。最新主线 Windows 正式全量 1496 项通过、13 项按平台跳过；用户手动重启服务后，OpenSSL 以项目 CA、`peach-win.local` SNI 与主机名严格校验通过，HTTPS `/healthz` 返回 200、0.7.13、writer、非只读；未替换生产 EXE。本轮未触发真实关注检查，未写真实 ledger、候选或媒体。
- 2026-09-01 关注页加载与来源标签语义收口：静态标题立即显示，Skeleton 只覆盖慢内容并按最终横向网格／居中面板占位；Spinner 与 Loading Dots 不再串成第二段等待。Rule34.xxx 检查从来源帖子页记录 `general`／`artist`／`character`／`copyright`／`metadata`，`blender`、`3d`、`3d_model` 实测均为 `metadata`；卡片、顶部筛选和在线标签页只接收来源明确标为 `general` 且通过第二层通用词清理的标签，旧行未知类型不再猜测，详情仍显示全部标签并按类型着色。关注详情舞台在 `.followlist` 中跨满整行，管理作者标题去掉 `Collection` 容器后缀。最新主线组合后 Windows 正式全量 1421 项通过、13 项按平台跳过；生产托盘与 80／443 子服务已重启，项目 CA、SNI、主机名严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，本轮三个前端资源哈希与当时已合入源码一致。内置浏览器实测桌面舞台／列表均为 1166 px、视频 774×435，手机可用宽度 380 px 下舞台 348 px、视频 346×194，二者均为真实 `/follow-stream?id=2841` 且横向溢出 0；关注管理标题显示 `Billyhhyb`，导航相对内容区居中。旧 Rule34 行需下一次用户主动检查后才补齐来源类型；本轮未触发检查、未写真实 ledger、候选或磁盘媒体。
- 2026-08-31 “定位源文件”反馈已统一复用 Geist 语义：等待态只在触发按钮内显示 Spinner，以 `aria-busy` 阻止重复请求但不禁用按钮；成功在右下角短暂 Toast“已在资源管理器中显示”，只有失败原因留在内容流。Windows `catalog` 492 项通过；在当前 HTTPS 页面拦截 `/api/reveal`、替换为候选 `app.js` 后，1280×800 与 390×844 的请求等待期详情内容高度均保持 460.5625 px，页面总高度分别保持 1210 px 与 1415 px，状态行持续隐藏，26×26 按钮尺寸不变且 Spinner 可见。项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读；本轮未调用真实定位接口、未写真实 ledger、未替换生产入口。
- 2026-08-31 JAV 官方封面规范化流程已写入项目技能并完成首轮全库正式执行：960 个非 FC2 JAV 番号中，824 张封面全部可解码，136 个仍缺失；缺失补抓新增 17 张，已有封面按像素面积升级 156 张，其中 Prestige 官方 API 133、DMM CDN 16、MGS 7。宽度不超过 800 的封面由 429 降至 274，宽度至少 1200 的由 305 增至 447；`ABW-232` 当前为 Prestige 官方 1024×690。流程先复用 Javinizer 原始证据，再量 DMM modern/legacy、Prestige、MGS 与历史精确 URL，按主机保持 1.5 秒限流；AVBase 遇 Cloudflare 即放弃且不绕过。正式 `metadata` 184 项通过，抓取批次退出码 0；封面与逐条来源日志已更新，批前日志备份为 `cover-fetch-log.pre-workflow-20260831.csv`。本轮不写 ledger、不改媒体文件，脚本与生成封面无需重启生产服务。
- 2026-08-31 播放器第十六轮确认 Chrome 早已能铺满，Firefox 失效的根因是同一普通 selector list 混入 Firefox 不识别的 `:-webkit-full-screen`，使整条全屏规则在 Firefox 作废；现在用 forgiving `:is(...)` 包住标准、WebKit、Gecko、Video.js、`data-peach-fullscreen` 与 full-window／祖先全屏状态，普通视图仍为 `contain`，全屏统一 `cover`。同时修复误推 GitHub 暴露的两项 CI：登记 YouTube 参考快照的新哈希；文件名清理按 ledger 路径形态选择 `ntpath`／`posixpath`，不再在 macOS 临时目录拼出反斜杠路径。Windows 正式全量 1329 项通过、13 项按平台跳过。Firefox 自动画面验收因 Windows 浏览器控制通道尚不能执行 URL 安全策略而未取得，不能以源码测试代替；本机生产部署与 GitHub Actions 结果待本提交集成后核验。本轮未写真实 ledger、未替换 EXE。
- 2026-08-31 播放器第十五轮撤回上一轮未经画面验证的根因判断：用户确认仅把全屏 `.vjs-tech` 改成 `cover` 没有生效。当前复测片源 `1275` 经只读 `ffprobe` 确认为 1920×1080、SAR 1:1，连续 8 秒 `cropdetect` 都是完整 1920×1080，已排除片源内嵌黑边。全屏状态现在除 Video.js／原生类外，还监听 `fullscreenchange`、`enterFullWindow`、`exitFullWindow`，把 `isFullscreen()` 同步为 `data-peach-fullscreen`；CSS 同时覆盖 `body.vjs-full-window` 回退，不再依赖单个类名的同步时序。Windows `catalog` 466 项通过。Chrome 已取得真实 `/item/1275` 普通画面截图；自动化的真实用户点击、DOM 和 CDP 连续超时，Playwright 按钮点击又不能提供 Fullscreen API 所需的用户激活，因此修改后的全屏画面仍未取得，不能声称视觉验收通过。未写真实 ledger、未替换 EXE。
- 2026-08-31 JAV 封面升级与标题显示已完成：已有封面可按宽度进入重探队列，只有新候选像素面积更大才原子替换，失败、同清晰度或更小候选均保留原图与既有成功日志；卡片、详情、沉浸、高清目标和集合队列统一显示“粗体大写番号 + 已有标题”，不显示媒体扩展名，但 API 仍保留真实文件名。真实无写入 POC 确认 DMM 数字版仍可得 2184 px、DUGA 已知 `ABW-232` 原图为 1000×674；AVBase 返回 Cloudflare 验证页，未绕过，DUGA 批量映射需正式 Web API 应用 ID。Windows `metadata` 172 项、最新主线 `catalog` 468 项通过；全量 1328 项中本批相关项通过、13 项按平台跳过，唯一失败为主线既有的 YouTube 参考截图登记哈希不一致。改动已合入 `master`；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 `app.js` 与 `master` 哈希一致，无需重启。内置浏览器两次打开生产详情页均在 20–30 秒超时，因此修改后桌面与 390×844 画面未取得。本轮未替换现有封面，未写 ledger 或磁盘媒体。
- 2026-08-31 播放器第十四轮按用户补充语义修复全屏画面填充：问题不是 Fullscreen API 未进入，而是全屏 `.vjs-tech` 仍沿用普通视图的 `object-fit:contain`，4:3 等非视口比例片源因此留下黑边。普通详情页继续 `contain` 保全内容，只有全屏改为 `cover` 覆盖 `100vw × 100vh`，接受边缘裁切以换取画面铺满。同步并发集成后的最新主线再跑 Windows `catalog` 466 项通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 CSS 与 `master` 的 SHA-256 同为 `17D7187E3B6B408048F78502F53CAB3E9CBCEF3365C53C46E452B0C58896AECC`，无需重启。Chrome 能新开真实 `/item/26701`，但刷新、DOM snapshot、截图和 DevTools `Runtime.evaluate` 分别在 15–20 秒超时并重置连接，因此修改后全屏画面和 computed style 未取得；未写真实 ledger、未替换 EXE。
- 2026-08-31 播放器第十三轮按用户截图纠偏：设置子层返回键继续保留 48 px 命中区和 32 px 官方箭头，只把内部悬停／按下圆形收为 40 px，避免越过 57 px 标题栏；音量 tooltip、音量面板和进度条分别锁定为 4／3／2 层，volume bar 允许越界显示，百分比提示不再被蓝色进度线遮挡。变基当前主线后 Windows `catalog` 466 项通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 CSS 与 `master` 的 SHA-256 同为 `3346BDBF67C1B6E6374879A7C8D85E981E24AEF6C511552C949E116F9D4617BB`，无需重启。本轮没有取得可控浏览器修改后画面，因此最终视觉验收未取得；未写真实 ledger、未替换 EXE。
- 2026-08-31 播放器第十二轮按 YouTube `e937390a` 锁定源码纠偏：设置子层不再复用全站开放描边 `i-check`，改为官方 context menu 内嵌的 24×24 填充 path，消除选中项黑色三角形；隐藏 Video.js 已播进度末端的当前时刻 tooltip，只保留鼠标位置 `vjs-peach-seek-preview`。音量悬停改由整个外层的 `:after{inset:4px}` 同时覆盖图标和滑轨，52 px 滑轨容器固定为 40 px 高，2 px 轨道绝对定位到 `top:50%`，12 px 圆点随之居中。变基当前主线后 Windows `catalog` 466 项通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 HTML／CSS／JS 哈希与 `master` 一致，无需重启。Chrome 能枚举 YouTube／Peach 标签，但接管 YouTube 标签和一次恢复均在 30 秒超时，因此修改后的 Chrome DOM、computed style 与最终画面验收未取得。本轮未写真实 ledger、未替换 EXE。
- 2026-08-31 播放器第十一轮直接复用 YouTube 当前 `e937390a` 官方 `www-player.css` 与 `base.js`：中央播放／暂停改为仅在用户手势后的反馈层，初始加载使用官方 64 px 双半圆旋转器结构和时序；氛围模式、播放速度、清晰度、前进／返回及中央播放／暂停图标使用本版本源码路径。音量按官方 `40 + 52 + 3 + 16 = 111 px` 展开几何重排，2 px 滑轨与 12 px 圆点垂直居中；设置主页改为 48 px 行和独立 32 px 箭头列，子页选项使用 35 px 左缩进、左侧选中标记，不再渲染 1080p 右侧符号。变基当前主线后 Windows `catalog` 465 项通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 CSS／JS 哈希与 `master` 一致，无需重启。Chrome 能枚举 YouTube／Peach 标签页，但读取页面资源和接管标签页连续超时，因此本轮修改后的 Chrome DOM、computed style 与最终画面验收未取得。本轮未写真实 ledger、未替换 EXE。
- 2026-08-31 元数据候选保留 MetaTube 模型中 Peach 先前丢弃的目录证据：`provider_id`／`content_id` 与标题、原题、片长、导演、厂牌标签、封面／海报／截图／预告 URL 进入同一来源候选和 `/review`，但不映射真相字段、不下载 URL，来源片长也不覆盖媒体探测值；审批 provenance 同步保留稳定来源身份。Pigo v1.4.6 对 512 张真实头像无写入 POC 检出 488 张，现有 OpenCV Haar 检出 313 张；Pigo-only 抽样出现无脸误报，且其纯 Go 部署优势不适用于 Peach，故不引入依赖，OpenCV 暂留，后续只 POC 当前 YuNet 与 MetaTube 主脸聚类。正式 `metadata` 168 项、最新主线 `catalog` 466 项通过；变基后全量 1318 项中本批相关项全通过、13 项按平台跳过，唯一失败是当时 `master` 已存在的 YouTube 参考快照与登记哈希不一致，本分支未修改这两个文件，未擅自接受外部参考变更。未写真实 ledger、未重建历史候选、未部署或重启生产。
- 2026-08-31 播放器第十轮按 YouTube `e937390a` 官方 CSS、既有实时 DOM 证据与用户截图纠偏：静音键固定在 40 px 音量容器内，音量／时间改用内缩 4 px 的白色 10% 悬停层；底栏播放／暂停和音量图标具备按下缩放，音量 40→111 px 保留 200 ms 展开。进度轨道由约 4 px 放大到 6 px，蓝色圆点由隐藏放大到约 20 px；设置换为播放器专用图标、整行悬停和严格 40×24／20 px 开关几何；中央 78 px 播放／暂停加入 1 秒放大淡出反馈；全屏固定到视口四边并使用 `100vw × 100vh`。Chrome 扩展复核后确认已安装且能同时加载 YouTube／Peach，首次“未安装”判断已撤回；但 computed style、CDP DOM 根节点和生产页刷新三条路径均超时并重置控制会话，因此本轮新增 Chrome 样式与最终画面验收未取得。Windows `catalog` 462 项通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 CSS／JS 哈希与 `master` 一致，无需重启。本轮未写真实 ledger、未替换 EXE。
- 2026-08-30 播放器第九轮按实际 YouTube `e937390a` DevTools 证据与用户截图纠偏：时长恢复 40 px 黑色圆角胶囊，点击可在已播／剩余时间间切换；音量展开背景在普通、影院和全屏三态保持一致；设置主页采用 274 px 面板、48 px 行、56 px 图标列，子页改为 57 px 标题栏和 32 px 白色返回钮。音量、画中画和全屏改用本地 24×24 播放器专用 SVG，并按静音与全屏状态切换。桌面与 390×844 实际 DOM 验收均无横向溢出，影院音量 hover 实测由 40→111 px 且背景、滑轨完整；变基最新主线后 `catalog` 462 项全绿。项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 HTML／CSS／JS 哈希与 `master` 完全一致，无需重启。本轮未写真实 ledger、未替换 EXE。
- 2026-08-30 播放器细节再次按实际 YouTube `e937390a` DOM／CSS／JS 和用户第八轮截图纠偏：底部播放／暂停改为独立 CSS 图形并在 40×40 圆钮居中；音量容器允许提示越界，只隐藏滑轨，40→111 px 展开完整；左侧圆钮与右侧 200×40 胶囊按用户亮色视频样本加深为黑色 60%。中央删除左右 10 秒键，只保留桌面 68×68、移动端 56×56 的单个深色播放／暂停圆钮；影院模式使用展开／收回两枚图标，并同步「影院模式／默认视图」和 `T` 提示。桌面实际画面、暂停态、音量展开和影院两态均经 DevTools 验收；390×844 实测中央按钮 1 个、右侧 152×40、画中画隐藏、横向溢出 0。变基最新主线后 `catalog` 457 项全绿；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 `app.css`／`app.js` 哈希与 `master` 完全一致，无需重启。本轮未写真实 ledger、未替换 EXE。
- FastAPI 是唯一 Web server；Ledger 保存资产、规范身份、行为和复核决定。CloudDrive 经挂载点进入，扁平 creator/studio/tag 字段只作兼容投影；Stash 的适配层已删除，历史断言以 `source='stash:*'` 留在账本里。
- 本地浏览器支持 MP4/WebM/Ogg；AVI 等由 `TranscodeService` 缓存成 H.264/AAC MP4。远端 MP4 默认走标准 Range，显式 HLS 使用关键帧对齐片段，失败会回退 Range，永不改写原媒体。
- 首页、详情、实体资料、标签管理、照片、播放列表、统计、口味、复核、回收站、关注管理和关注观看共用同一 SPA 与 JSON 契约；Logo、侧栏「首页」和沉浸模式关闭统一清除分类、搜索与 JAV 筛选，不能出现地址已经回到 `/`、接口仍只请求 JAV 的隐藏状态。首页默认恢复为稳定随机，换批才更换种子；再次点击当前排序会取消该排序并回到随机，设置中的默认排序也提供「随机」。桌面和 390×844 手机均在验收范围内。
- 2026-08-30 「管理 → 垃圾文件」已有独立 `/junk-files` 路由，并按带图标的视频、图片、压缩包、音频、网址和其它文件分类；所有卡片都直接提供「不是垃圾」与「移入回收站」，视频不复用馆藏标记控件。小图模式的两个卡片操作只显示图标并保留提示，避免按钮文字超框；大图模式保留完整文字。顶栏多选和卡片选择标记在此页可用，批量栏只显示当前视图适用的「不是垃圾／重新判断」与「移入回收站」。「不是垃圾」按 asset 保存为可撤销的复核决定，可在「已排除」中重新判断，不扩散成来源或域名白名单。真实压缩包分类只读实测 6 项，其中 5 个 `Mib19.com*.zip`，桌面与 390×844 手机布局、44px 手机操作按钮、路由空态和控制台均已验收；未点击操作、未写真实 ledger。
- 普通多女优卡片叠放前 3 个头像，只显示第一位姓名和真实总人数；JAV 小图是整页版式，混入的非番号作品也统一为标题、身份、标签三行固定高度，大小保留在身份行，超出逐行省略。详情与沉浸模式仍保留完整或扩展后的出演信息。
- 当前源码的在线追更支持 FANBOX、Patreon、SubscribeStar、kemono/coomer/pawchive、rule34video、rule34.xxx、Rule34 Paheal 和 f95zone；FANBOX 可选 Cookie 用于公开详情被验证页拦截时复用用户会话，Gofile Premium API token 可展开 FANBOX/F95 外部文件页。两份凭据只发给各自站点。SimpCity 仍被 DDoS-Guard 阻塞，Peach 不绕过机器人验证。官方渠道只读取公开免费发布，不绕过登录或付费墙。
- Rule34.xxx 来源 ref 大小写不敏感；迁移会合并分批添加造成的重复来源并保留条目状态。F95 的 `Collection(s)` 后缀不进入作者名，五个来源可归到同一作者组。
- 关注作者显示名与头像优先取经过固定主机校验的官方 FANBOX/Pixiv 页面，归档站只作回退。官方主页只出现一个明确作者名时，主页账号自动记为有出处的关注别名；已有映射尤其是人工作决定不会被覆盖，只有名称相似时仍只给建议。只支持真实历史分页的来源才显示「抓更早一页」。
- reader 的 `/review` 通过严格 Peach CA HTTPS 读取 writer 的归一化 JSON 并原子缓存；决定按钮和所有关注写操作仍锁定，不同步 SQLite/WAL 或原始候选目录。
- macOS Ledger 同步在共享根判为 `offline` 时会先通过 NetFS 尝试挂载 `peach-sync`，再重新判定；挂载失败才保留原离线结果，过程中不停止服务。默认使用 `peach-win.local`，可由 `PEACH_SHARED_SMB_*` 覆盖；对应主机没有钥匙串记录时快速失败，不弹出阻塞认证框。
- Windows 托盘与 macOS 菜单栏均提供「同步开发进度」。Windows 会先快进并运行正式测试；涉及打包输入时先构建和检查新 EXE，再退出旧托盘完成可回滚替换，失败不会提前停止现有服务。
- 搜索使用 FTS5 trigram，短查询回退 LIKE 并覆盖规范名、别名和检索词。搜索历史在 reader 写入被拒时降级到页面内存，不产生未处理异常。
- 回收站的单项删除和清空共用 `purge_assets()`：先处理媒体文件，再提交 ledger；删不掉的文件保留可见状态并回报，避免静默遗留。
- 复核页覆盖元数据、创作者标签、Logo、头像、身份、番号目录、FC2 证据和媒体失败。抓取与 AI 结果仍是候选，批准后才写真相字段。
- 浏览历史增量采集使用 SQLite backup API 与固定版本 `browserexport==0.4.4`，一致性读取本机 Chrome／Firefox／Zen／Safari，也接受 Google Takeout ZIP 和 browserexport SQLite／JSON／JSONL 导出；原始 URL、标题和导出文件只留本机私有目录，聚合候选不写 ledger。

## 本批修正与验证

- 2026-09-01 Python 版本口径收敛到实际运行的 3.14，CI 从 4 个 job 减为 2 个。`requires-python` 原本写 `>=3.12`，CI 矩阵按这个声明测「下限 + 实际版本」，但这个下限没有使用者：README 让两台机器都用 3.14 建 venv，本机实测 3.14.7，Peach 不发 PyPI 也没有第三方按 `requires-python` 安装。历史上唯一一次 3.12 单独红（run 33368707206，Windows 3.12 失败而 3.14 通过）是预览并发生成那条计时敏感用例的偶发，不是版本不兼容。同时把「测试里的临时目录先 resolve」写进 AGENTS.md 硬门槛：这个坑一周内烧了两个分支且没有任何脚本或测试守它。AGENTS.md 87/90 行仍在预算内。Windows 正式全量 1638 项通过、13 项按平台跳过。本轮未写真实 ledger、未替换生产入口。
- 2026-09-01 播放统计补上滚动历史，并修掉 GitHub Actions 上四个 job 全红。连接速度、网络活动、缓冲健康各保留 24 个 1 秒采样槽，新样本从右侧进入；缓冲健康低于 5 秒红、5–15 秒橙、其余浅绿，以 30 秒为健康上限。这部分从未集成的 `agent/codex/player-buffer-bars`（证据采集于 2026-08-29）移植，分支落后 552 个提交已废弃；原分支另一半「强化 Video.js 时间轴」**未采纳**，master 的时间轴此后已按更新的 `youtube-player-controls-user-screenshot` 实测证据重做，照搬会回退已上线结果。CI 四个 job（Windows／macOS × 3.12／3.14）失败原因完全相同且与 Python 版本无关：测试拿未 resolve 的临时目录去比对被测代码 resolve 后的真实路径，而 runner 的临时目录都是别名（macOS /var 软链到 /private/var，Windows 的 RUNNER~1 短名展开成 runneradmin），本机没有这层别名所以一直全绿。已用目录 junction 在本机复现原始失败（含 `restart_tray` 相同的 `StopIteration`）并验证修复。`test_agent_worktree` 的两项同类失败属并发中的另一分支，本轮未动这两个文件。Windows 正式全量 1636 项通过、13 项按平台跳过。本轮未写真实 ledger、未替换生产入口，播放统计的真实画面验收未取得。
- 2026-09-01 修复 JAV 大图混排与番号显示清洁：根因不是多女优头像，而是 `PBD390` 等缺连字符的历史 code 被判成非 JAV，封面比例退回 16:9；现在只有片商、发行日或 performer／studio／series 实体证据成立时才接受紧凑番号，`RAIKUN325` 等账号仍排除。API 保留原始 `name`／`code`，另给规范 `display_code`、清洁后的文件名回退标题和 `edition_badges`；`-C`／`-CH`／`-SUB` 显示框选“中字”，`-U`／`-UC`／`Uncen` 显示“无码”，`AI去码`／破解证据显示“无码破解”，不再把后缀或括号推广域名当标题。真实 ledger 只读全库审计覆盖 2282 条带 code 视频：1413 条确认 JAV，16 条缺连字符但有发行证据，301 条有版本徽章；三张截图目标分别投影为 `PBD-390`、`CJOD-175 + 中字`、`CJOD-158`。文件名清理 dry-run 扫描 75499 条并产出 49 条 115 改名计划，目标含 `PBD00390.mp4 → PBD-390.mp4`、`CJOD-158[fuckbe.com].mp4 → CJOD-158.mp4`，无撞名；两次运行前后 ledger SHA-256 均为 `8FBA698C5CC362A762DD9A45A62074A283DD3448A5E758DA2DA75F43144E8D85`。正式全量 1433 项通过，13 项按平台跳过；真实改名、0023 迁移与生产部署尚未执行，修改后实际浏览器画面未取得。
- 2026-08-31 修复 JAV 详情缺正式标题与官方 Tag 身份：新增 `asset.catalog_title`／`original_title` 真相字段和 0023 迁移，Javinizer 标题／原标题进入逐字段候选并经人工批准写入，FTS、列表、详情和 JAV 显示统一消费；Javinizer 官方来源 Tag 在详情显示“官方”。旧实现只从文件名截取番号后的文字，目录证据没有真相字段，因此文件名只有番号时标题必然为空。相关推荐按 OpenAver 固定 revision 的 Tag IDF／结构化共同点方向独立实现，并增加 MMR 多样性、稳定 seed 和中文原因；真实 ledger 20 个种子只读 POC 每个均返回 12 条、两次一致，运行前后 SHA-256 不变。fc2cmadb 登录态实测旧文章 `3701252` 仍返回当前 Inertia `article`／`comments` 结构，现有解析协议可继续使用；未读取或导出浏览器凭据。`metadata` 185 项、`catalog` 497 项、正式全量 1423 项均通过，13 项按平台跳过。0023 尚未应用到真实 ledger，候选尚未重建或批准，生产尚未部署。
- 2026-08-31 关注页三批修复与在线标签词表，全量 1352 → 1377 项通过、13 项按平台跳过。①rule34video 多清晰度：该站把每档写成独立字段（`video_url` 是最低档，其余是任意编号的 `video_alt_urlN`），只读第一个就永远播 360。档数由来源决定不写死——取证那条给四档，线上另有含 2160 的五档条目；2160 按站点自己的写法显示 4K。videojs 的 `qualityLevels` 帮不上忙，那套只认 HLS/DASH 自适应轨道，这里是几个各自独立的 mp4，只能换 src 并接回进度。②详情图开大图：复用女优页那个灯箱，按 slide 归一化——它原本写死 `/photo?id=`，在线图没有 asset id 套不进去；在线图没有本机路径可揭示，「在资源管理器中显示」那个面板整个收起。③筛选与计数同源：药丸上的数字原本来自一句全库 SQL，列表由浏览器在已加载的几页上筛，两个口径，所以换筛选条件列表变了数字不变。作者和标签都不是库里的列（`author_key` 要经实体绑定和别名映射推导，tags 要从 metadata JSON 解析去噪），在 SQL 里重写这两套推导等于把同一语义实现两遍；整库 3054 条连解析一起实测 15ms，筛选整个搬到服务端。顺带消掉 counts 里排除 rule34video 和无资源 f95zone 的两段重复扣减。可选项（facets）另按全库口径算，否则选中一个作者后作者栏只剩他、切不回去。分页也移到筛选之后——以前先分页再筛，选个冷门作者要反复点「加载更多」。
- 2026-08-31 修一族「离开一个页面没收干净」的中间态。用户复现路径：在 `/tags?view=alphabet` 点一个标签（目录被它筛住），再进关注页，标题上面还挂着「白虎 ✕ 全部清除」。根因不是漏了一行——`enterManagementSurface()` 早就存在而且是对的，但关注、播放列表、复核三个页面各自手抄了它的一部分，都只抄走 `tiers` 和 `tagbar`，都漏了 `combo`，也都漏了 `loadRequestSeq++`（少了它，一个在途的目录请求返回后还能把筛选条重新画到新页面上）。三处改回调用它，`openStats` 里那行冗余手抄一并删。测试断言五个整页视图都必须调用它、手抄那对元素的地方只允许两处，再有人抄就变红。这不是后端问题：真正缺的是「进入一个页面该重置什么」没有单一入口，约定不是门槛。
- 2026-08-31 标签页与左侧抽屉列出在线标签。本地是 ledger 里已入库作品的中文标签，在线是关注库里还没入库的更新带的 booru 英文标签，计数含义、类别划分和点击后去哪儿三者都不同，所以标签页加「本地／在线」范围切换、抽屉单开一节。字母表因此各得其所：实测 3582 个在线标签全是 ASCII，能分出 `#` 和 A–V 共 22 组；本地全是中文，做字母表只会得到一个「中文」分组。统计与关注页筛选条共用 `_follow_facets`，不是另写一份，否则标签页说某标签 12 条、点进去只有 9 条，两个数都「对」只是口径不同——counts 上已经犯过一次。`/api/follow/tags` 形状刻意与 `/api/index` 一致，标签页现成的分页、搜索和「载入更多」换个地址就能用。点一个在线标签直接进「关注 · 这个标签」，为此把 `provider`、`tag`、`status` 也放进关注页 URL——以前只有 `author` 和 `media` 在里面，其余只活在模块级全局，离开再回来还按着、刷新就丢；现在 URL 是唯一真相源，重置由它推导而不是一串手写赋值。途中修掉两个撞车：目录筛选原本无条件从启动 URL 读，`/follow?tag=blender` 会顺手把目录也筛成 blender（查询参数属于它所在的路由，改成只从目录 URL 读）；抽屉底部那句通用绑定选择器是 `.chip`，会把关注标签一并接管，点下去等于按 undefined 筛目录、跳回首页（收窄到 `.chip[data-key]`）。浏览器实测：在线标签页字母表分组、点击直达 `/follow?tag=blender` 且计数从 3054 收到 581、抽屉芯片同样直达、`#combo` 全程为空。本轮未写真实 ledger、未替换生产入口、未部署，只启停 8909／8910 两个开发实例。
- 2026-08-30 修正共享 Note 的信息图标：此前 Note SVG 漏了 Lucide 所需的圆端点，长度 `.01` 的 `i` 圆点不可见，圆环也只有默认细描边；现在与图片详情的正确规则一致，使用 2px 描边、圆端点和圆连接。用户提供的错误图标截图与 Vercel Notifications 参考差异已写回证据；1206×911 与 390×844 实测圆点清晰、计算样式为 2px／round／round、页面横向溢出 0、控制台警告／错误 0，重放最新主分支后 Windows 正式全量 1307 项通过、13 项按平台跳过。8924 临时预览已更新，不写真实 ledger、不部署或重启 80／443。
- 2026-08-30 口味页“不合口味”归因和底部隐私说明改用共享 secondary Note：圆圈信息图标、整行中性表面、16px 内容列对齐和 `role="note"` 语义一致，不再以悬空小字挤在面板边缘。Vercel Notifications 当前登录态截图、实时 DOM／CSS 读取超时和 Peach 有意差异已登记；没有真实后续动作，因此不伪造链接。1142×911 和 390×844 实测两条 Note 均无自身或页面横向溢出，窄屏自然换为两行，控制台警告／错误 0；Windows `catalog` 457 项通过。8924 临时预览已更新并恢复浏览器记录视图，不写真实 ledger、不部署或重启 80／443。
- 2026-08-30 普通详情与在线关注播放器继续按锁定的 YouTube 证据收口：左下、右下五个可见按钮统一为 40×40 圆框并共用基线，左上统计圆钮与加载速度固定同高；设置浮层只列真实的氛围模式、播放速度和清晰度，氛围模式沿用 32×18 当前帧取色与模糊扩散并新增全局持久开关；影院按钮与 `T` 在不进入全屏时扩展播放器，详情和队列移到下方。1440×900 与 390×844 的真实在线关注视频实测浮层、开关、1.25× 速度、影院布局、无横向溢出和控制台 0 错误；生产页五个圆框均为 40×40、设置浮层 340×192／22px 圆角，氛围状态已恢复为开启。集成后 `catalog` 455 项全绿；全量 1295 项只剩与本批无关且前两轮通过的预览并发计时用例在高负载下瞬态失败，随后 `catalog` 中同域复核通过。生产 `app.js`／`app.css` 严格 HTTPS 哈希与 `master` 一致，项目 CA `/healthz` 返回 0.7.13、writer、非只读；纯前端资源无需重启，本轮未写真实 ledger、未替换 EXE。
- 2026-08-30 完成统计／口味预览反馈：全站最深底色从 `#020408` 提亮为 `#080A0D`，覆盖关注管理、输入与登录表面；关注页直接在线播放新增独立观看事实，不再要求先保存成 asset，真实 ledger 尚未应用新迁移。统计页用系统盘、资源盘、115 与 PikPak 的使用空间表替代单一系统盘，标签 Top 排行在桌面分成两栏、手机单栏；重复分隔线、管理导航／标题对齐、口味数据源说明间距和 `Tag` 文案一并修正。Vercel Table／Home 的已取得原则、浏览器超时与有意差异已锁定。重放到最新主分支后 Windows 正式全量 1303 项通过、13 项按平台跳过；临时副本的 1280×900 与 390×844 实测页面横向溢出 0、管理导航相对内容区居中、标题与阅读列左边一致、存储表 4 行、标签排行 2／1 栏、可见面板唯一且控制台警告／错误 0。更新后预览保留在 8924；未写真实 ledger、未替换 EXE、未部署或重启 80／443。
- 2026-08-30 继续收口口味页反馈：标签、创作者、女优、常访问网站、浏览候选与数据源统一为 8px 间隔、1px 边线、6px 圆角的独立小卡片；数据源标题、正文、底部隐私说明统一 16px 节奏，标签来源最后一行不再重复下划线。Peach 创作者／女优继续使用实体头像与代表作品回退；临时预览补齐头像缓存和只读接触表入口后，女优 20／20、创作者 19／20 取得图片。浏览器创作者没有可信个人头像时显示其聚合来源网站图标，API 不返回原始 URL；缓存契约升为 v3，避免 24 小时旧结果遮住新字段。桌面和 390×844 均无横向溢出，catalog 457 项通过；只更新 8924 预览，未写真实 ledger、未部署或重启 80／443。
- 2026-08-30 依据登录态 Vercel Analytics 与 Speed Insights 当前页面重做统计／口味两页：统计页用库存、观看、标签覆盖和系统盘四个真实指标构成横向指标轨，单项主详情与标签／最近观看／标签来源 tablist 同层切换；口味页以浏览器记录／Peach 内部作为真实证据来源，摘要、主详情和 Tag／创作者／女优等排行随来源切换，不伪造体验分或时间图。实际浏览器首轮发现显式 `display:grid` 覆盖 `hidden`，已补隐藏门槛和页面源测试。真实 ledger 与浏览历史的一致临时副本在 1280×900、390×844 下实测：统计详情、统计维度、口味来源和口味维度均只显示当前面板，页面级横向溢出 0，指标轨只在自身内部滚动，控制台错误 0。重放到当前 master 后 Windows 正式全量 1290 项通过、13 项按平台跳过；一次性预览和临时副本已删除，本轮未写真实 ledger、未替换 EXE、未部署或重启 80／443。
- 2026-08-30 修复详情播放器与在线保存回归：关注详情的保存按钮在异步请求前固定 DOM 引用，Firefox 不再因 `event.currentTarget` 归零报错；关注视频改用首页详情同一套 Video.js，控制栏统一垂直几何并新增真实 `qualityLevels` 清晰度菜单，单一路源只显示实测分辨率／原画，不伪造 720p、480p。关注 API 只新增安全 MIME 投影，原始媒体 URL 仍不进入 JSON。详情右栏将“整列背景”和“76vh 可滚动内容”拆层，本地、在线资产、关注、图片及合集分支共用，背景不再随内容上限截短。正式 Windows 全量 1277 项通过、13 项按平台跳过；临时账本浏览器实测保存 POST 200、按钮变为“已保存”、在线视频为 Video.js，桌面与 390×844 控件对齐、清晰度菜单在视口内且无横向溢出。改动已合入 `master`，Windows 托盘及 80／443 源码服务已重启，项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，生产 `app.js`／`app.css` 已包含修复。生产 `/item/128423` 在 1280×720 实测右栏、媒体和网格行同高 435.375px，横向溢出 0；生产关注详情导航被媒体请求拖至浏览器控制超时，最终生产 DOM 复测未取得，未用静态资源代替该结论。本轮未替换 EXE、未触发关注检查、未写真实 ledger；独立 8918 进程保持不动。
- 2026-08-30 修正管理页与小图模式的剩余几何回归：重复文件和回收站的标题说明改用同一个 `pagelede` 元素与样式，空回收站不再保留隐藏计数栏高度；统计页删除空 `statshead` 并移除容器上内边距；Progress 暗轨改用 `line-soft`，资源同步去掉父网格之外的额外上边线、上内边距和上外边距；JAV 小图对整页混合卡片固定标题／身份／标签三行，缺字段保留行高，长字段省略。Windows 正式全量 1273 项通过、13 项按平台跳过。改动已合入 `master`，生产 `app.css`／`app.js` SHA-256 与源码一致；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，纯前端资源无需重启。内置浏览器能在 1 秒内列出已加载的“统计 · Peach”标签，但 `goto`、Playwright evaluate、CDP evaluate 和截图均在 10–30 秒超时并重置控制连接，计算样式和桌面／手机视觉验收未取得；这不是页面未加载的证据，未用静态测试冒充视觉通过。本轮未替换 EXE、未重启服务、未写真实 ledger。
- 2026-08-30 按 Vercel Geist Command Menu／Search Input／Spinner／Loading Dots 的锁定源码证据修正等待与反馈语义：设置 Dialog 恢复 350ms 进出动画，导航项目靠左，滚动容器用双侧稳定 gutter 保持视觉居中；关注查找固定搜索图标前缀，请求时原位换 Spinner，来源筛选保留“全部来源”，添加与检查等用户动作使用 Spinner，后台抓取使用 Loading Dots。历史分页的 400／404／空页按来源契约归为“没有更多历史内容”，不再写失败状态、推进游标或显示红色，并提供可关闭的中性 Note。Windows 正式全量 1272 项通过、13 项按平台跳过；临时账本的 1280×720 与 390×844 浏览器实测横向溢出 0，设置卡中心与视口中心一致，打开／关闭动画和再次打开无残留，控制台 0 错误。只启动并关闭 8912 开发服务，未部署、未重启生产、未写真实 ledger。
- 2026-08-30 依据锁定的 Vercel Geist Fieldset／Scroller／Empty State 官方 HTML／CSS 复核人工复核和管理空态：复核卡改为真实 fieldset，统一 440px 高，20px 正文内边距、单一纵向 Scroller 与最小 56px 固定 Footer；关注管理的“添加关注／关注列表／凭据”标题条实测均为 56px。回收站、普通零结果、重复文件、复核、高清版、播放列表与关注空态统一使用共享组件，标题下说明和可选动作不再拆散或套双层空卡；重复页与回收站在 390×844 下距上方摘要均为 16px、横向溢出 0。新增导航代际 token；浏览器连续快速切换疑似广告→重复→复核→重复后最终路径与内容一致，旧网格 0 项、控制台 0 错误。42 张创作者复核卡实测全部 440px，高内容卡 Scroller 为 1566／333px 且显示底部渐隐。Windows 正式全量 1265 项通过、13 项按平台跳过。浏览器 Fieldset DOM 已取得，但官方页计算样式读取 20 秒超时并重置；未取得面已登记，尺寸使用同日锁定哈希的官方资源，没有目测补值。本地预览只使用真实 ledger 的临时副本，未部署、未重启生产服务、未写真实 ledger。
- 2026-08-30 依据锁定的 Vercel Geist Note／Progress／Switch 官方 HTML 与登录态 Analytics DOM 复核近期 UI：新增原生 ES module 组件层，资源同步、口味加载和关注检查明细统一为持久 error Note，关注页历史故障明确归为可关闭 Banner；统计页改成有独立 header/body 的 Analytics 式数据面板，所有进度条使用真实 value/max、`progressbar` ARIA，指标分隔线位于进度条下；设置标题移出滚动正文，分隔线到达卡片两边；导航添加改成每项有图标、内部滚动、无入场动画的 listbox；JAV 三种版式改用共享 radio name 的 Geist Switch，布尔遮挡继续使用 Toggle。新增 `peach-web-ui` 项目技能、引用快照和源码门槛，后续 UI 改动必须先复用现有组件与证据。Windows 正式全量 1262 项通过、13 项按平台跳过；最终菜单焦点修正后的 `catalog` 434 项复测通过。本地 1440×900 与 390×844 实测面板、分隔线、菜单图标／边界、Switch、横向溢出 0 和控制台 0 错误。Speed Insights 登录态页面因浏览器导航超时未取得，已明确登记，未用 Analytics 臆测替代。本轮只启动并关闭 8901 开发服务，未部署／重启 80／443，未写真实 ledger。
- 2026-08-30 修复 JAV「小图 · 整张封套」多人卡片过高：根因是该版式只改封面比例，仍复用普通卡片的 3 头像、两行标题、大小、观看次数和标签，字段换行后单卡会撑高整行。现在只在 `jav-small` 下显示前 2 个头像，标题和身份各 1 行，次要元数据留到详情；大图、预览图和详情不变。分支重放到最新 `master` 后正式 `catalog` 432 项全绿；生产页面 1280×720 与 390×844 实测多人卡片头像为 2、元数据区 40px、次要字段不占布局，控制台无错误。纯前端资源实时生效，无需重启，本轮未写真实 ledger、未替换生产 EXE。
- 2026-08-30 修复首页在线来源与导航回归：已保存的 `location=online` 资产不再被默认缩略图门槛静默排除，来源与卡片统一使用 RSS 图标；在线详情只引导到关注页「已保存」，不会把网页 URL 当媒体串流。首页导航会显式清掉 `jav=1`，JAV → 首页后 URL 与高亮同步恢复。隔离 worktree 在 rebase 到最新 master 后 `catalog` 430 项全绿；Windows 托盘与两个 venv 子服务已安全重启，生产 API 的 `loc=online&thumb=1` 返回 1 条、叠加 `jav=1` 返回 0。1280×720 与 390×844 浏览器实测在线卡片、RSS、JAV → 首页、详情门槛和无横向溢出均通过；项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer。本轮没有触发关注检查或写真实 ledger，也没有替换生产 EXE。
- 2026-08-30 将 Vercel Geist `MiddleTruncate` 从首批组件升级为项目门槛：审计现有全部截断后，8 个资源标识调用点统一显式使用 `data-middle-truncate`，新增覆盖复核原视频文件名、关注附件名和 Mix／分卷／播放列表队列文件名；标题、说明、人名、标签和状态值继续末尾省略。页面源测试登记当前全部 31 个 `text-overflow:ellipsis`／line clamp 选择器，新加、删除或改名任何截断都会失败，修改者必须先分类；共享队列的语义文本另用 `data-truncate-end`，样式表末尾强制中间省略目标使用 `text-overflow:clip`，防止两套算法叠加。实现基线全量 1257 项通过、13 项跳过；两次主线更新后最终 `catalog` 432 项通过。项目 CA 严格 HTTPS `/healthz` 返回 0.7.13、writer、非只读，线上 `app.js`、`app.css` 与 `middle-truncate.js` 哈希均和最新 `master` 一致，无需重启。本轮浏览器已打开生产高清版页并取得标题，但 DOM、可见结构、截图与 CDP 读取均连续超时，桌面／手机新增表面的视觉验收记为未取得；未写真实 ledger、未替换生产入口。
- 2026-08-30 复核 `master` 相对 `origin/master` 的 9 个界面提交：保留有用户回执或可复现证据的 Toggle、卡片面、danger 按钮、标题截断、空态、来源筛选语义与作者块；纠正四类臆测／失效实现。①凭据说明改成可聚焦 Tooltip，打开时才设置 `aria-describedby`，Escape 可关闭，250px 最大宽度并按视口夹取，不再把页面撑出横向滚动条。②添加关注沿用 Projects 工具栏的“输入占余宽 + 方形筛选 + 右侧主操作”，来源菜单固定在视口内、内部滚动且无展开动画。③作者别名按 Geist Collapse 的测量高度和 200ms `ease-in-out` 实现内容／chevron 过渡，并处理快速反向点击。④删除无证据的设置面板入场动画、未使用的 loading-dots／menu 动画，以及没有有效高度约束却注入每张复核卡的 Scroller。官方 bundle SHA、登录态页面测量与 Peach 差异已登记；重放后 `full` 1256 项通过，最终基线上的 `follow` 446 项、`catalog` 430 项也通过。临时数据库上的桌面 1440×900 与手机 390×844 浏览器验收均为横向溢出 0px；筛选菜单 250px、距视口至少 8px、无动画，Tooltip 也保持在视口内，作者别名展开语义正确。生产服务在开工时未运行，本轮未替换生产入口。
- 2026-08-29 按用户回执修三个「修过又坏」的回归与一批 Vercel 对齐项。回归根因：①组合标签时顶部 pill 的按下态判据是整串相等（`filterState.tag===t.k`），多标签永远不全亮，改为按逗号拆分逐个命中；combo 芯片显示裸 key（「骑乘」）而 pill 显示显示名（「骑乘位」），芯片改用 tagLabel、操作仍用原始 key，✕ 清除后 pill 按下态同步（实测双选双亮、单删单清）。②抽屉/窄栏导航的激活态是 buildBars 时一次性算好的，管理页不跑 buildBars，切页后停留上一个页面的按下态（实测 /stats 下「首页」还亮、「管理」不亮）——路由钩子新增 `paintNav()` 每次路由变化对两侧按钮重算 `navOn`（实测 /stats 两侧点亮管理，/follow 点亮关注）。③限宽布局按用户回执整体改回（与上方导航和标题版式不适配），测试加反向断言防止无重排回归。其余：设置卡对齐 Vercel 后台顶格排版（标题 16px/600，分隔线经负边距通到卡边）；页面标题升到 32px/650/-0.01em（对齐 Geist H1 尺度）；关注页失败提醒提供 dismiss 按钮（会话内记忆，失败时才出现）。follow 432 项、catalog 409 项通过；浏览器实测上述全部行为。纯前端改动实时生效。
- 2026-08-29 按用户回执再修三处：限宽只保留统计与口味（复核是卡片网格、高清版是宽表格，窄列反而挤压，恢复全宽）；设置面板分组改为框体隔开（每组建卡：发丝边 + 微亮底 + 8px 圆角，对齐后台设置卡样式）；口味仪表按窗口做会话内缓存——命中直接渲染不再等「正在分析…」，换窗口/导入/移除/显式读取才打接口并写回缓存，慢响应带序号防止旧数据覆盖新渲染。catalog 409 项通过；浏览器实测：SPA 内口味↔统计来回切换仪表即时渲染、接口只调一次，复核/高清版全宽，设置面板 6 组框体与审查遮挡开关正常。纯前端改动，资源实时生效，无需重启。
- 2026-08-29 按用户设计复核（对齐 Vercel Geist 与后台 dashboard，取证 `docs/reference-snapshots/vercel-geist-semantics-measured.md`，浏览器 getComputedStyle 实测）修一批界面与反馈：检查失败的报错带明确原因——rule34.xxx 限流（HTTP 200 + "limit of 60 requests every 60 second(s)" 正文）识别为「触发频率限制：每 60 秒最多 60 次」，429 报「请求过于频繁」，网络错误带异常摘要，非 JSON 带正文摘要；连接器统一走 `_upstream_reason`/`_body_snippet`。检查失败报告条改为 danger 语义（红发丝边 + 微红底，去掉左侧粗条），关注页 `.fwarn` 补 14px 下间距消除贴身；来源行 ok/error 改 Geist 低饱和状态徽章 `.sbadge`（顺带修掉 `--success` 变量从未定义、`.resourcesources .online` 静默失效的既有 bug）；「全部标记已看／全部忽略」按用户回执回到计数行内（inline-flex，不另起一行）；清空回收站改 danger 色按钮。审查遮挡移出导航栏：开关移入设置面板「安全」组，默认关闭，只在与会审查内容的模型的测试会话开启，规则写入 AGENTS.md；主题字体栈全部补 CJK sans（`Bahnschrift,sans-serif` 短栈在中文 Chrome 落宋体）。设置面板选项分六组（浏览/播放/搜索/关注/界面/安全）；统计/口味/复核/高清版限宽 1004px 居中（对齐后台设置列 914px 实测），浏览页保持全宽，页面表面经 `body[data-surface]` 驱动。PikPak 图标改用官方 `mypikpak.com/apple-touch-icon.png`。follow 432 项、catalog 408 项通过；浏览器实测：默认不遮挡、设置开关链路生效、徽章配色、批量同 行、限宽 1004px、PikPak 图标加载。
- 2026-08-29 关注 tag 反转义修复（`70a6216`）已切换到生产：正式 EXE 替换为 SHA-256 `1EA6C18BEC9D30ED8D5D54F5F4B473F1400B3DCC9426CB37D1B3623AABF4A481`，上版备份为 `dist/Peach/Peach.pre-70a6216-20260829-181842.exe`（SHA-256 `FCD54E557D9FE36FE3D3B0FFC50E486701CA5B990334A280F203691BF94EC9B0`）。打包入口在真实数据根确认 22 个迁移、0 待处理；全量 1215 项通过后按 `replace_windows_tray` 受控换装，替换助手曾因备份文件未预建拒绝一次（服务树与托盘已停、生产短暂离线约 1 分钟），补齐备份后换装成功。新托盘重新拉起服务，80/443 由新子进程接管，`/healthz` 返回 0.7.13、writer、非只读。线上 `/api/follow` 脏 tag/标题 0 命中，`miqo'te`、`y'shtola` 正常渲染；关注详情点 `miqo'te` 胶囊精确过滤 2 条。替换期间未触发关注检查、未写 ledger。
- 2026-08-29 修关注页 tag 转义（用户报告 `'` 显示成 `&#039;`）：rule34.xxx 的 dapi 返回 HTML 转义形态，实体原样入库、页面二次转义后显示成字面量；真实账本 132 行中招（131 行 rule34xxx 的 tags、12 行标题、3 行 source URL），全部 `new` 状态。入库侧连接器统一反转义 tags 与 source URL（归组键只取 path，既有分组不受影响）；读取侧 `_item_tags` 与条目标题在出口幂等归一——旧行不写账本即恢复正常显示与筛选，同一标签的脏/净写法合并为同一身份。其余主要 tag 面排查干净：Rule34Video/Paheal 走 BeautifulSoup 已解码，目录 `/api/index`（tags/performers/studios）与 `asset_tag` 词汇 0 命中，fanbox 行的 `&` 是合法字符非实体。follow 430 项通过。账本存量实体的物理清理待另行授权；当前读取层已归一，展示与筛选不受影响。
- 2026-08-29 按用户复核意见改两处关注反馈：检查回执 toast 带具名动作「去看更新」（点击关掉自己并跳转关注流），统计文案收掉悬空的「条详情」——它曾被当成可点的链接；关注管理的批量操作（全部标记已看／全部忽略）从计数行内的裸链接改为独立 `.fbulk` 行的 `.fbtn` 次级按钮，与本页「检查全部／去看更新」同一套控件语言。follow 427 项通过；生产页面实测：toast 动作点击后正确跳转 `/follow`，批量按钮渲染正常，全程未触发真实检查、未写 ledger。
- 2026-08-29 关注检查反馈对齐 Geist toast 处方（取证：`docs/reference-snapshots/vercel-geist-toast.md`）：「检查了 N 个来源」改为右下角 toast（挂 body 直下，整页重画冲不掉，自动消失、hover 暂停、右上角关闭，`aria-live="polite"`）；失败只发一句短 toast，原因明细留在管理页内的持久行（`.fcheckreport`，左侧红条 + 恢复入口），关注页的持久失败状态走重排后的 `.fwarn`（图标 + 行内链接，修掉链接前 10px 死间距与缩小字号）。全站转圈动画并成一套 `peach-spin`：检查更新、检查全部、抓更早的一页、扫描差异、同步并清理、读取浏览器、顶栏换一批统一为按钮 busy 类 + 图标自转，「抓更早的一页」不再丢图标。新增顶栏「审查遮挡」开关（`#censorBtn`）：默认开启——封面与预览图以原始画面进截图会被截图管道的审查拦下（关注页实测），localStorage 只记用户显式关闭；开启后全站内容图与视频（含 videojs 海报层、悬停预览、沉浸模式、关注流封面/详情海报/图片墙）统一重糊去饱和压暗，悬停预览同时不再启动；品牌标与来源 favicon 豁免。上一轮遗留的 4 处 catalog 源码契约（settingscard 主题覆盖顺序、滑杆轨道 pill 字面量、换一批重置种子精确串、搜索聚焦环 color-mix 断言）一并修正，`follow` 426 项、`catalog` 405 项通过。
- 2026-08-29 按检视报告做架构优化，六批独立提交、每批全量回归。`web_contract.py` 3294 → 1899 行：资源对账拆为 `web_resource_sync`，复核队列拆为 `web_review`，两者都用 Protocol 只声明各自需要的能力；拆分暴露的三处循环依赖靠下沉纯规则解决（`dir_expr`／`GENERIC_PHOTO_DIRS`／`photo_set_title` → `catalog_rules`，`_resolve_entity` → `entities.resolve_entity`）。`web_logic.py` 改名 `catalog_rules.py`——那 206 行没有一行是 web 的却被数据层依赖，倒置的是名字不是方向；新增 `test_module_layering` 锁住分层。follow 域：删除 ADR-0007 的 RSS 适配层与 `feedparser` 依赖（ADR-0007 已标 Superseded by 0019），provider 元数据收进 `follow_providers` 单点登记表（凭据策略刻意留在 `CREDENTIAL_GUIDE`），八个连接器的取回骨架抽成 `_BaseConnector._request()`，探测能力转正为 `probe`／`fetch_json`／`parse_json`。`w_item_tag` 改走 `write_transaction`，顺带修掉「除资产不存在外任何 execute 抛出都既不回滚也不关连接」。`test_rm_web.py` 2428 → 1512 行，测试随领域同批拆出；拆带 patch 的域时 patch 目标必须跟着代码走，否则静默失效。全量 1188 → 1206 项通过。
- 2026-08-29 清理仓库与顶层目录。分支 79 → 4（`master`、`agent/codex/taste-analysis`、`preserve/windows-review-ads-20260822`、当前会话分支），工作树 76 → 3；删掉的 73 个是已合并进 master 的 agent 分支，另有三个被 master 等价实现超越的分支按 SHA 记录后删除（`62e7618`、`cb4b044`、`d2ae77d`），SHA 存于 `../attic/README.md`。顶层 12 个目录收敛为 4 个 `peach-*` 运行时目录加一个 `attic/`：`peach-` 前缀此后专属 ADR-0017 定义的 `peach-app`／`peach-data`／`peach-sync`／`peach-worktrees`，其余按 `builds`／`evidence`／`instances`／`tools`／`reviews` 五类归入 `attic/`，日期统一写成 `YYYYMMDD-主题`。规则与本次映射写在 `../attic/README.md`，不进 Git（顶层非仓库）。删除的只有空目录 `verification` 与 `__pycache__`；账本副本、复核产物、取证归档一律保留。

- 0.7.13 按 Beeg 当前资料页证据重排女优、厂牌和关注页：头像与简介在前，随后是链接、关联艺人、标签、视频／图片 Tab 和内容；关注页移除来源说明，图片按原比例进入无黑边瀑布流，混合更新仍同时进入两个媒体视图。普通 Mix、分卷、播放列表、关注视频合集和多媒体统一为“左侧媒体、右侧详情、下方横向切换带”；右侧与切换带共用背景，脱盘占位铺满首行，Mix 保存入口位于标题与摘要信息块右侧。接口继续复用既有 `media_kind`／`media_items`，没有新增依赖、迁移或真实 ledger 写入。Windows `follow` 412 项、`catalog` 403 项、正式全量 1186 项通过，分别 1／0／13 项按平台跳过；开发服务浏览器实测关注页视频 220、图片 13，图片视图为 4 列自然高度瀑布流；真实 `Mix 31631` 为 29 个横向切换项，媒体、详情首行同高 558.2px，背景连续，保存入口距信息块 8px，1124px 视口无横向溢出。390px 视觉验收因当前浏览器不提供视口切换接口而未取得，响应式源码契约已覆盖。
- 0.7.13 将照片灯箱的详情入口收成单独的 30×30 圆形 `i`，去掉文字按钮；详情打开后点击面板和入口以外的位置会先收起提示，不误关整个灯箱；不足 1 MB 的图片大小改用 KB，避免显示成 `0 MB`。Windows 的“在资源管理器中显示”从 `explorer /select` 改为原生 `SHOpenFolderAndSelectItems`，完整路径仍只由服务端按 asset id 查询，不送到浏览器。首次验收只量到图标中心偏差为 0 px，遗漏了 `info` 圆点因缺少 SVG 圆头而几乎不可见；现已补齐 Lucide 描边端点，并把实际字形截图纳入桌面和手机验收。使用生产相同 SVG 与 CSS 的页面在 1280×800 和 390×844 下均实测为 30×30 按钮、20×20 图标、水平与垂直中心偏差 0 px，圆点与竖线清晰分离，无横向溢出且控制台 0 错误；Windows `catalog` 403 项通过。集成后生产 `/app.css` 已返回圆头规则；OBOKOZU 的现存图片灯箱在桌面和 390×844 下再次实测相同尺寸、中心偏差 0 px、圆点可见、无横向溢出且控制台 0 错误。OpenSSL 以项目 CA 和 `peach-win.local` 主机名严格校验为 `Verify return code: 0 (ok)`，HTTPS `/healthz` 返回 0.7.13、writer、非只读，无需重启或替换 EXE。临时文件实测接口返回成功，资源管理器窗口地址确认为目标文件所在目录；手机详情面板为 360 px、点图片区域可关闭提示。用户确认截图中的图片均为已删除广告，本批未恢复图片、未把这些缺失文件判为代码故障，也未写真实 ledger。
- 0.7.12 修复艺人／厂牌资料页从深链或刷新进入时只看 URL 参数、因此漏掉 JAV 三种版式按钮的问题：页面现在从实际返回作品的 `is_jav` 推断资料页能力，并只给 JAV 卡片应用封面比例。照片查看器的缩放加减号改用本地 SVG，桌面和手机实测按钮与图标水平、垂直中心偏差均为 0 px；新增图片详情，显示文件名、来源和大小，切换图片时同步更新，并复用既有 `/api/reveal` 按 asset id 在资源管理器定位，客户端仍不接收或提交原始路径。照片 DTO 只新增安全的 `location`，没有迁移或真实 ledger 写入。Windows `catalog` 402 项、正式全量 1183 项通过，13 项按平台跳过；1440×900 下资料页三种版式按钮均可见，390×844 下详情面板 360 px 且位于 390 px 视口内，两端均无横向溢出和控制台错误。生产托盘已受控重启且继续使用原 0.7.11 EXE，80／443 由新子进程接管；HTTP 与项目 CA 严格 HTTPS `/healthz` 均返回 0.7.12、writer、非只读。生产资料页实测三种版式按钮均可见；真实 115 图片详情显示文件名、来源、大小和 asset id 定位入口，1280 px 与 390×844 下均无横向溢出，缩放图标中心偏差为 0 px。未点击生产定位按钮，未写真实 ledger。
- 0.7.11 将管理画像入口统一命名为“口味”；资源同步不再占用独立管理页，改为统计页末尾，旧 `/resource-sync` 深链自动替换为 `/stats#resource-sync`。同步配置按 Vercel 表单卡重排：主信息在上层，安全说明和 36px 次级操作放在带分隔线的底栏；扫描结果共用一套 12px 外框与内部 1px 分隔线，来源、汇总和确认操作不再各自嵌套卡片或压住边缘。按钮具备明确 hover／focus 和原位旋转忙碌态；窄屏规则提高到组件作用域，避免被后置基础规则覆盖。Windows 正式全量 1181 项通过、13 项按平台跳过。临时数据库的 1280×720 浏览器实测结果为三列来源、两列汇总、底栏按钮完全位于 1176px 外框内；390×844 下来源与汇总均为单列，外框 358px、底栏 356px、按钮 324×36，文档与视口同为 390px，无横向溢出。
- 0.7.11 正式 EXE 已替换为 SHA-256 `FCD54E557D9FE36FE3D3B0FFC50E486701CA5B990334A280F203691BF94EC9B0`，上版备份为 `dist/Peach/Peach.pre-0.7.11-20260829-004620.exe`；打包入口在临时 ledger 上确认 22 个迁移、0 待处理。80／443 均由新进程恢复，OpenSSL 以项目 CA、`peach-win.local` SNI 与主机名严格校验返回 `Verify return code: 0 (ok)`，固定到实际监听地址后的 `/healthz` 返回 0.7.11、writer、非只读，`/stats` 与旧 `/resource-sync` 均为 200。生产浏览器实测管理栏只有“口味”且无独立资源同步入口；1280×720 下配置卡 1166px、底栏 1164px、按钮 102×36，390×844 下卡片 348px、底栏 346px、按钮 314×36、无横向溢出和控制台错误；旧链接原位进入 `/stats#resource-sync`。未点击扫描、未执行同步应用。停止旧 SQLite 连接时主文件 SHA-256 由 `27F540B8F6B296F544EA2B250D159C0D7A2DFA6F59FA962F20AAE4CB2B63A8E3` 变为 `14F30D85F33631B74E99D9E1F48BBA090F57CD1D12AD3804AD47C2CC00FB8F60`，时间点与停服一致且重启后 WAL 为 0 字节，符合关闭连接时的 WAL 回写；只读复核为 `integrity_check=ok`、外键违规 0，asset 81555、follow_source 14、follow_item 1551、entity 8177。

- 0.7.10 修复设置弹层滚动后标题和关闭操作一起离开视口的问题：标题栏现在粘在弹层自己的滚动容器顶部，配合 `overscroll-behavior:contain` 与焦点滚动预留；桌面滚动 540px、390×844 手机滚动 620px 后标题栏位置分别保持 37px／63px，页面无横向溢出且控制台错误为 0。按当前 Vercel Geist Materials 与 Web Interface Guidelines 重新核对全站组件语义：普通操作 6px、状态标记 4px、菜单与 Modal 12px、Tag 胶囊，媒体空间导航和头像保留圆形；搜索、播放列表、设置、顶栏、管理、详情、关注和移动端共用 token，并新增源码门槛拒绝 `transition:all` 与局部漂移。正式 Windows 全量 1178 项通过、13 项按平台跳过；新 EXE SHA-256 为 `A8CA4CCC2FDFEEDB004C9AC9D521F94609FF37E6B798DB718DB2B9245EBF7FD5`，上版备份为 `dist/Peach/Peach.pre-0.7.10-20260828-232322.exe`。首次替换启动与旧服务退出发生竞态，重启新托盘一次后 80/443 恢复；项目 CA 严格 HTTPS 返回 0.7.10、writer、非只读。未触发关注检查或其他 ledger 写操作，部署前后 ledger SHA-256 均为 `A03E3B450B2721B7FB8078D4A3751C498750B1A8608C66F49CCBD96AE949BF6F`。

- 0.7.9 将 Rule34.xxx 封面从 250×141 `preview_url` 升级为官方 `sample_url`，历史行按同一 bucket/hash 只读升级，不改 ledger；Rule34 Paheal 图片直接读原图，视频按需双并发抽取并缓存最大 1280px JPEG。抽帧在开头 30 秒用 FFmpeg `blackframe` 选第一张黑色像素低于 98% 的帧，版本化缓存键会自动淘汰旧黑帧，全黑或超时继续回退低清封面。真实生产样本：Rule34.xxx 为 1080×1920，Paheal 图片为 1920×2964，Paheal 视频封面为 1280×720、黑像素占 28.9%，旧缓存已在请求后原子替换。关注域 410 项、Windows 正式全量 1176 项通过，分别有 1/13 项按平台跳过。正式 EXE SHA-256 为 `2719F6D3CC9951DC11FB387A49CFEF1ED19DC4A7CCEF27100C44A47A53833F65`，上版备份为 `dist/Peach/Peach.pre-0.7.9-20260828-224816.exe`；22 个迁移、0 待处理，80/443 已恢复，项目 CA 严格 HTTPS 返回 0.7.9、writer、非只读。生产浏览器首屏实测 Rule34.xxx 3840×2160、Paheal 1280×720，无横向溢出与控制台错误。未触发关注检查，ledger SHA-256 保持 `A03E3B450B2721B7FB8078D4A3751C498750B1A8608C66F49CCBD96AE949BF6F`。
- 0.7.8 修复 JAV 大图的两个同批回归：Mix 卡片现在与同一网格里的作品共用大图比例和官方封面，不再单独掉回 16:9；生成缓存清理新增数据库所属 data root 边界，临时数据库即使漏配默认缓存根也不能越界删除真实 `peach-data/generated`。事故复核确认 0.7.6 开发期的 FastAPI 清空回收站测试虽然使用临时 ledger，却漏传 `cover_root`／`stream_root`，因此把真实 `covers` 当成孤立缓存；目录在 2026-08-28 21:12 被清空。旧抓取日志仍有 484 条证据，其中 243 张成功、241 张未取得；新增 `fetch_jav_covers.py --restore-successes` 只按成功记录的原 URL、原尺寸原子恢复，不重新探测来源、不写 ledger。与并发关注封面改动重放后，Windows 正式全量测试 1175 项通过、13 项按平台跳过。243 张成功记录已全部按原尺寸恢复，日志 SHA-256 仍为 `EED7DA6D1D522260BBF060C0B47FDF22E98FDDF202AE4DA65FB435A5FE309B39`，ledger SHA-256 仍为 `A03E3B450B2721B7FB8078D4A3751C498750B1A8608C66F49CCBD96AE949BF6F`。用户手动重启服务后，项目 CA 严格 HTTPS 返回 0.7.8、writer、非只读；`ABP-993` 封面返回 200 `image/jpeg`。生产页面桌面实测 Mix 与普通 JAV 卡片均为 383.3×547.6、比例 0.7；390×844 下 `ABP-993` 为 348×497.1、无横向溢出，浏览器错误 0。生产 EXE 未在本批替换。
- 0.7.6 新增「管理 → 资源同步」：只读扫描逐来源核对 `local`、115、PikPak，整源离线时跳过；确认应用会重新核对，把网盘上已删除的 asset 先移入 Peach 回收站，并清理没有正常馆藏引用的截图、海报、图片缩略图、转码、流媒体片段、按 asset 生成的头像和共享番号封面。候选 CSV、来源证据、实体头像和 Logo 明确排除。详情页原有目录同步也改为先入回收站，不再直接删账本；清空回收站后统一清理派生缓存。共享封面、离线整源、临时测试库不触碰真实 generated 根均有数据层门槛；桌面和 390×844 响应式源码契约已覆盖。正式 Windows 全量测试 1157 项通过、13 项按平台跳过；未在生产执行应用清理，真实 ledger 未写，但正式测试的缓存根隔离缺口实际清空了真实 `generated/covers`，更正与修复见 0.7.8。后续生产只读扫描因约 8 万条记录和 3673 个目录在 5 分钟后超时，现改为可离页的后台作业、逐来源进度、同目录单次枚举和固定 8 路目录元数据读取；应用只重查本轮缺失候选，目录暂时不可读不进入回收站。
- 艺人、厂牌、创作者、系列资料页的作品区统一复用首页排序：换一批、推荐顺序、每日轮换、随机、评分、高潮计数、观看次数、时长、体积、最近入库、最近看的；照片瀑布流和管理队列不添加无适用语义的排序。资料页 API 继续保留完整别名供搜索，但页面不再展示本地化规范名的纯拉丁转写；番号卡片优先显示规范女优实体，不再被旧 `asset.creator` 罗马字投影抢占。实体解析改为精确规范名优先、仅接受唯一别名，修复 `飯岡かなこ` 被 `森泽佳奈` 同名别名抢走的问题。390×844 实测页面宽 380、无横向溢出，排序行 348/710 px 可横滚；艺人、厂牌、创作者、系列均有 10 个排序按钮，JAV 厂牌另保留 3 个版式按钮。集成后正式 Windows 全量测试 1145 项通过、13 项按平台跳过。生产托盘已重启并重新拥有 80/443；项目 CA 严格 HTTPS 返回 0.7.5、writer、非只读，`释爱丽丝` 的纯拉丁别名不再展示，`飯岡かなこ` 路由到实体 8559。
- 系列规范名已按 271 个代表番号重新读取 r18dev 同一响应中的官方日文翻译：261 个请求成功、10 个明确返回 404、0 个可重试错误或限流。真实 ledger 写入前用 SQLite Backup API 建立 `database/ledger.pre-series-name-localization-20260828-205228.db`，SHA-256 为 `07AAFBA70B08936D77DF98BAD5DA792617D707794EDBA3922C00207280A783D3`；253 个系列改名、1 个英文实体并入已存在的日文实体、425 条 `asset.series` 兼容投影同步，旧英文名保留为不展示但可搜索的别名。另有 7 个官方日文值与当前值相同、14 个没有可验证日文证据，均未猜译。asset 81555、asset_entity 152990 保持不变，series 289→288、entity 8178→8177、entity_alias 1526→1781；备份和现库均 `integrity_check=ok`、外键违规 0，现库系列投影不一致为 0。项目 CA 严格 HTTPS 已确认 0.7.5、writer、非只读；旧英文路由 `Raw Creampies`、`Aesthetics of a Facial`、`FALENO Compilation` 分别解析到 `なまなかだし`、`顔射の美学`、`FALENO総集編`，英文别名不进入展示列表。
- 最近 12 小时 Windows 应用日志没有 Peach 的 1000/1001 崩溃事件；本轮 18:31 的 `Peach.exe` 退出是为加载已测试源码而执行的受控重启。重启后托盘与两个服务进程持续存在，80/443 正常监听；当前没有异常自动退出证据。
- 真实 ledger 只读姓名审计发现 22 个含拉丁字母的 performer 规范名；其中账号名和 `nao.`、`Sumire` 等发行名不应机械翻译，能由 r18 非拉丁发行名与映射条目共同确定为本地化倒退的只有 3 个：`Hitomi Hoshiya`→`星谷瞳`、`Mika Fukuyama`→`福山美佳`、`Ako Shiraishi`→`平沢すず`。本地化脚本已拒绝把非拉丁规范名倒退成拉丁映射，并能按一致的 r18 发行名恢复这三类错误。`かわいゆい` 与 `河合ゆい` 有独立头像文件和身份资料，当前把后者登记为前者别名、r18 外部引用和头像来源属于错误关联；真实 ledger、头像和来源引用尚未写改，等待单独授权后按备份与完整性门槛修正。

- 0.7.5 将馆藏里同番号且带连续唯一后缀的 A/B、1/2、CD/Disc/Disk/DVD/Part/Pt/Vol 分卷派生为一张叠层卡片，标为“2 卷”而不是 Mix；卡片显示合计时长与大小，详情用“分卷”右侧队列按 A→B／1→2 切换。每卷仍是独立 asset 和播放会话，不拼接媒体、不写 ledger；重复标记、缺卷或字母/数字混用会拒绝自动合卡，重复文件页原有安全判定不变。Windows 真实 ledger 只读 POC 识别 82 组、197 个资产，含 `DVAJ-495-A/B`、`FC2-1464245-CD1/CD2` 等现有命名；`catalog` 375 项、正式 Windows 全量 1137 项均通过，13 项按平台跳过。
- 设置面板把圆角外框与内部滚动层分离，右侧滚动条不再把圆角切平。左侧导航可直接鼠标拖拽排序、逐项隐藏，并从全部顶层入口及统计、人工复核、疑似广告、重复文件、回收站、关注管理、高清版等具体页面中选择添加；窄栏和筛选抽屉继续共用同一份顺序。正式 Windows 全量测试 1133 项通过、13 项按平台跳过；临时账本浏览器实测隐藏 JAV、添加统计、拖拽事件链重排和 `/stats` 独占高亮均生效，桌面右侧上下圆角完整，390×844 无横向溢出且保留上下移动回退按钮，控制台无错误。集成后生产页面实时读取新资源，无需重启：HTTP 回环页实测外框 `16px` 圆角、内层独立滚动、导航拖拽属性、隐藏按钮和 7 个具体页面添加选项均生效，控制台无错误；80/443 持续监听，项目 CA 严格 HTTPS `/healthz` 返回 0.7.4、writer、非只读。调试遗留的 `127.0.0.1:8915` 临时服务已按监听 PID 清理，生产端口未中断。
- 照片标签不再先显示固定 3:4 的图集封面，直接按原图比例进入分页瀑布流；目录分组继续留在 API 兼容字段和旧深链中。JAV 女优、厂牌资料页复用首页的大图／小图／预览图按钮，切换时重画已载入卡片，不重复取数。
- 厂牌长条 Logo 的方图补边不再只看四角颜色：原图只要含透明像素就继续透明，完全不透明时才取整圈边缘主色。旧算法曾把贴边字样误认成底色，导致 `PREMIUM` 整张铺蓝、`HEYZO` 白字消失；17 个受影响的已安装 Logo 已从原图备份重建，错误版本另行备份。`/logo` 改为缓存但每次重载按 ETag 校验，避免固定 URL 继续显示替换前的旧图；项目 CA 严格 HTTPS 已确认两张响应与重建文件逐字节一致。
- 沉浸模式按用户提供的 YouTube Shorts 桌面截图重排：竖屏使用居中的 9:16 播放舞台，横屏使用在整个视口视觉居中的 16:9 大舞台；48 px 圆形操作按钮与文字分离，作者头像、身份和标题留在页面左下；390×844 手机继续使用全视口播放器。保留 Peach 的三项本地反馈、混合横竖屏队列和完整画面优先，不复制评论、分享、Remix 或订阅。
- 用户生产截图确认上一版横屏舞台贴右导致视觉失衡；同一截图还显示 HTTPS 服务 PID 47788 同时预读三部已切走的视频，子进程 PID 43080 正在转码 `IDBD-446_C.mkv`。根因是沉浸模式没有复用详情播放器已有的可取消 `session` 契约。本批为每个沉浸视频分配独立会话，并在切片、关闭、失败和页面离开时通知服务端取消。取证后 PID 43080 已自行退出；PID 47788 是正常 HTTPS 服务，不是下载器，3 秒复核读写增量均为 0，未强行终止任何进程。
- 正式 Windows 全量测试 1123 项通过、13 项按平台跳过。临时静态页面在 1440×900 下实测横屏舞台 922×518、中心偏差 0 px、无横向溢出；390×844 下舞台为完整视口且无横向溢出。浏览器控制台无错误；尚未重启生产服务。
- 沉浸模式补齐画面点击播放／暂停，桌面左右键按设置步长快退／快进；手机单击切换播放状态，同侧双击左右半区快退／快进，并保留横划进度与竖划切片。详情播放器错误文案改为画面中央：390×844 临时账本实测错误文字框与左上加载速度徽标不重合，普通点击可暂停／恢复；内置浏览器不支持注入原生 touch 事件，手机同侧双击的真机触控证据记为未取得。
- `5926ff4` 已合入 `master`：关注页改用与首页一致的作者头像和标签胶囊，压缩顶部间距；管理页不再残留目录标题；`深喉咙` 统一为 `深喉`；非视频主附件也会从同一发布的图片附件取预览图。
- Fiu758 只登记为厂牌 Logo 候选发现来源，不作真相源。17 个已安装横条 Logo 已原样备份并无损补成正方形；5 个损坏图片保持不动并进入错误报告。
- 正式 Windows 测试入口跑完 1040 项，全部通过，13 项按平台跳过。桌面浏览器确认关注页顶部为 88px、无旧筛选控件、无横向溢出且目录标题隐藏；手机视口覆盖连续超时，截图验收为未取得，响应式源码契约测试已通过。
- 新 EXE 已部署到 Windows 生产入口，旧版备份为 `dist/Peach/Peach.pre-34932ec-20260827-190405.exe`。打包迁移为 21 个、0 待处理；80/443 已恢复到新托盘子进程，项目 CA 严格 HTTPS `/healthz` 为 writer，部署前后 ledger SHA-256 不变。生产 `/api/follow` 返回 277 个变体，其中 233 个带预览；新标签页确认首页式筛选、标题清理与桌面无溢出，手机视口覆盖仍未生效，手机截图验收为未取得。
- `58a4c4f`、`8da8c81` 已合入 `master`：Rule34.xxx 的 HTTP 200 空正文按零命中处理，管理页把查找结果合回添加区并统一标题字号；来源名称可回到原页面，同一作者只保留一个标题。多栏单位是作者组，组内来源保持单栏。正式 Windows 测试 1051 项通过、13 项跳过；生产桌面与 390×844 手机视口均无横向溢出，严格 HTTPS 为 writer，重启前后 ledger SHA-256 不变。
- `38dcc63`、`b3b028e` 已合入 `master`：关注筛选把「全部」放首位，来源只显示 favicon，作者和筛选横条支持鼠标拖动、滚轮与触摸横滚；卡片复用首页网格，操作按钮只在悬浮时出现，集合用 Mix 叠层和可滚动弹层，多选可批量保存、已看或忽略。批量接口兼容单项并在一个事务中处理所选条目。
- 更新后的正式 Windows 测试为 1051 项通过、13 项跳过。生产继续使用原 EXE，仅重启托盘加载源码；80/443、21 个迁移且 0 待处理、项目 CA 严格 HTTPS writer 均正常。桌面实测作者蓝框完整、筛选横滚从 0 到 640、悬浮操作与 Mix 不重叠、29 行集合弹层和单项多选正常；390×844 为 348px 单列且无页面横向溢出，手机截图捕获失败，视觉截图记为未取得。
- 浏览器验收在 Mix 入口尚被悬浮按钮覆盖时误触三次「标记已看」，`follow_item` 181、184、185 从 `new` 变为 `seen`；未保存到账本、未新增 asset。ledger SHA-256 从 `F8FBD87E...` 变为 `82D46DA2...`，需用户明确授权后才能恢复，当前不再写入。
- 关注名称查找会把下划线和连字符形式同时按分词形式查询；F95zone 站内索引仍未命中时，结果区给出同名 Google 查询入口，供人核对真实线程链接，不自动登记来源。正式 Windows 测试 1056 项通过、13 项跳过。
- `8bb4f91` 已合入 `master`：关注来源新增 FANBOX、Patreon、SubscribeStar；每条渠道以自定义勾选框控制是否参与更新检查，检查/移除改为单行图标；作者组在宽屏整体多栏，组内来源保持单栏并链接原页面；测试按 `follow`、`catalog`、`media`、`sync`、`metadata`、`tooling` 拆分，默认仍为全量。
- 合并当前主线后正式 Windows 全量测试 1083 项通过、13 项跳过。临时账本与 8912 端口的浏览器验收：1440px 为两个 467px 作者栏，390×844 为 320px 单栏；12 条来源均为 51px 单行，无页面或行级横向溢出；作者顶部边线均为 0，检查/移除均为 32×32 纯图标；SubscribeStar 开关可同步启停其检查按钮。临时服务和文件已删除。
- 真实 ledger 已授权应用 `0021`，并登记 InitialA 的 FANBOX 与 SubscribeStar 来源和 `FFXIVInitialA` 人工别名：FANBOX 首次发现 10 条，SubscribeStar 发现 7 条，其中新增 6 条、更新 1 条。当前 follow_source 12、follow_item 706、follow_author_alias 2，完整性正常。
- `d32f948` 已合入 `master`：FANBOX、Patreon、SubscribeStar 的官方主页只有一个明确作者名时会自动学习主页账号别名；多人主页不学习，自动证据不覆盖人工映射，模糊相似仍只给建议。关注域测试通过，全量 1086 项通过、13 项按平台跳过。
- 正式 EXE 已替换为 SHA-256 `83D5B176D2A53500D4697BF53589EE528FE9C872B7694EE2932D98C063EF3788`，上版备份为 `dist/Peach/Peach.pre-d32f948-20260827-215647.exe`；80/443 均恢复监听，项目 CA 严格 HTTPS `/healthz` 返回 200、writer、非只读，正式打包入口为 22 个迁移且 0 待处理。
- `a789efe` 已合入 `master`：关注卡片封面、标题、整张卡和 Mix 集合行统一打开 `/follow/item/{id}` 站内详情，不再以外站新窗口作为主交互；视频继续走 `/follow-stream`，来源页只留在详情侧栏。关注范围 358 项测试通过、1 项按平台跳过。正式 EXE 已替换为 SHA-256 `5231DF9898054FB1C62B87766994D06966CA09EBFF3146ED60F9630BC6B743B8`，上版备份为 `dist/Peach/Peach.pre-a789efe-20260827-221738.exe`；80/443、22 个迁移且 0 待处理、项目 CA 严格 HTTPS writer 和关注 API 均正常。生产页面实测无媒体与视频详情均不新增浏览器标签，关闭回到 `/follow`，桌面排版与首页详情一致；390×844 为单列且无横向溢出。浏览器自签 HTTPS 导航超时，视觉交互改由同一生产进程的 HTTP 回环入口验收；HTTPS 结论来自独立严格 CA 校验。验收未触发关注写操作，ledger SHA-256 仍为 `1B53408069EBCE5FB44156ABDA785D76A2E3C5DDABE0D30468A1DCB6D31CD600`。
- 迁移前已知的孤立 `follow_item` rowid 790 原本属于 `rule34xxx/initial_a`，缺失来源 id 恰好被新 FANBOX 来源复用为 12。2026-08-28 经授权，在一致备份 `database/ledger.pre-relink-follow-item-790-20260828-001728.db` 后将它重关联到现存 Rule34.xxx 来源 10；来源 10/12 的条目数由 100/11 变为 101/10，总数仍为 706。完整性与外键检查正常，严格 HTTPS 关注 API 返回该条为 `rule34xxx`、来源 10、状态 `new`。
- `b18e410` 已经由 `12fab62` 合入 `master`：关注详情的多视频集合改用首页 Mix 同款右侧队列，队列内直接切换；详情补回作者头像，来源收成标题旁外链图标，完整内容标签可点击并回到多选筛选。默认标签不再显示为全选，停用渠道保留在管理页但从关注流与计数中隐藏；InitialA 头像优先复用同作者官方 FANBOX 来源，最终回退为拉丁首字母；SubscribeStar 使用已验证的 32px favicon；FANBOX 子域链接的查找结果紧跟添加区显示。忽略操作统一改为划线眼睛，不再与关闭 X 混淆。关注域 360 项、正式 Windows 全量 1088 项均通过，1/13 项按平台跳过；1440×1000 与 390×844 浏览器验收均无横向溢出，多标签选择/取消、详情标签筛选、队列切换和管理页 FANBOX 查找均通过。
- 正式 EXE 已替换为 SHA-256 `40471E2DECF602EC72D5EAD548408C40AB0BD11AAF1FCEFDE492077E2BE70BC1`，上版备份为 `dist/Peach/Peach.pre-12fab62-20260828-011932.exe`；80/443 已由新进程恢复监听，打包迁移为 22 个、0 待处理，项目 CA 严格 HTTPS `/healthz` 返回 writer、非只读。生产关注 API 为 330 组、400 条，停用的 SubscribeStar 来源仍可管理但流内条目为 0；生产资源已包含 Mix 队列和新的忽略图标。部署前后 ledger SHA-256 均为 `8F0D4EE06379C406F601733EF2FDF21F7E9DA125C92DEAE211E73CD121E776A8`，本轮未写 ledger。
- `baa6c81`、`c07c2de` 已经由 `5d452b78`、`cddc23eb` 合入 `master`：F95zone 只把已验证的文件分发链接当成媒体，使用已保存的 F95 cookie 按当前 masked 协议解析同站跳转，cookie 不会发给 Gofile/Pixeldrain；关注卡片与详情只有在同组至少存在两个可直接播放的视频时才显示 Mix，线程回复数和一个外部文件页不再伪装成视频数。详情外链图标紧贴标题，说明文字使用不会被通用 `.fnote` 覆盖的独立间距。Gofile 官方内容接口仍需独立 Bearer token；两个实样返回 `error-notPremium`，因此只能确认外部文件页，文件列表与可播放视频未取得，不能据此生成 Mix。
- Windows 正式 EXE 已替换为 SHA-256 `63E84B344FD8900EED81D177DA2CDC31C26A83CE3358CFBA171F909052562348`，上版备份为 `dist/Peach/Peach.pre-cddc23eb-20260828-022410.exe`；主分支正式测试 1090 项通过、13 项按平台跳过，打包迁移 22 个、0 待处理，80/443 与项目 CA 严格 HTTPS writer 均正常。生产 DOM 实测 InitialA 单外链详情不显示 Mix，标题与外链图标间隔 5px；服务端已提供最终说明间距规则，但内置验收浏览器在禁用缓存后仍复用上一版 CSS，最终计算样式记为未取得。未触发生产“检查更新”，ledger SHA-256 与修改时间保持 `8F0D4EE06379C406F601733EF2FDF21F7E9DA125C92DEAE211E73CD121E776A8` / 2026-08-27 16:25:04 UTC。
- `f09e7aa` 已经由 `6ae030d` 合入 `master`：关注详情补齐真正的外链 SVG 图标，F95 已验证的 Gofile／Pixeldrain 文件页进入 `resource_urls` 并直接显示可点击链接；没有文件页时不再声称“已取得”。`/follow/item/{id}` 注册为 SPA 直达路由，刷新不再返回 404。名称查找会用归档 FANBOX 的数字 Pixiv 身份核对官方 FANBOX 资料，`LazyProcrastinator` 可发现 `https://lazyprocrast.fanbox.cc/`，不猜子域名；已有候选文案统一为“已经关注”。检查全部和单来源检查图标都在忙碌态使用同一旋转动画，但生产验收未实际触发检查更新。
- 正式 Windows 全量测试 1096 项通过、13 项按平台跳过；新 EXE 的打包迁移为 22 个、0 待处理。生产 EXE 已替换为 SHA-256 `BBDBB6EC7D3C2312E45F8F68B41BD5158749EF78D9A2C676F42FE7FED2D6A91F`，上版备份为 `dist/Peach/Peach.pre-6ae030d-20260828-031013.exe`；80/443 已恢复。项目 CA 的证书链和主机名校验通过，Schannel 因无法确认本地 CA 吊销状态而使用 `--ssl-revoke-best-effort`，`/healthz` 返回 writer，`/follow/item/190` 返回 200。
- 生产 API 共 330 组、400 条关注条目，4 条带外部文件页；item 565 显示 Gofile 与 Pixeldrain，item 190 返回真实 Rule34.xxx 来源 URL。生产浏览器实测标题旁外链图标为可见 16×16 SVG，文件页链接各带可见 15×15 图标，管理页查找可见官方 FANBOX 和“已经关注”，控制台无错误。验收未触发关注写操作，部署前后 ledger SHA-256 均为 `7389FC058C28651795E7AF77A7AD27CEED84A48E2B8AEF52767982E73957F80D`。
- `9051557` 已经由 `825348a` 合入 `master`：F95zone 回复只有在正文含论坛附件或已验证的文件站外链时才进入关注候选；引用块、纯讨论和普通网页链接会计入“跳过无资源”。附件直链同时作为预览图；历史纯讨论条目只在读取层隐藏，不删除 ledger。关注域 372 项、正式 Windows 全量 1100 项均通过，分别有 1/13 项按平台跳过。正式 EXE 已替换为 SHA-256 `D5447756E913704AD413FFFF7A57F1E10660D8B2CF7B3D57C2D6D2D8947372E7`，上版备份为 `dist/Peach/Peach.pre-825348a-20260828-095720.exe`；80/443、22 个迁移且 0 待处理、项目 CA 严格 HTTPS writer 和关注 API 均正常。生产流当前显示 151 组，其中 F95zone 资源条目 4 条；未触发检查更新，部署前后 ledger 哈希不变。
- `4dbd3c7`、`dcfae42`、`c495178` 已合入 `master`：版本由长期漏升的 `0.6.4` 升为 `0.7.0`；新增 Rule34 Paheal 来源、Gofile 独立 API token 输入与 Bearer 代理、FANBOX 正文外链和多图/多视频队列。外部媒体只经 `/follow-stream` 代理，token 不进入 URL 或关注 JSON。FANBOX `post.info` 若被验证页拦截，公开列表更新仍会入库并明确显示“媒体未取得”，不会拖垮整个作者来源。最终全量 1107 项通过、13 项按平台跳过；后续 FANBOX 修正的关注域 380 项通过、1 项跳过。
- 正式 EXE 已替换为 SHA-256 `59A8E43797EC6FDB13093465453DB09EF31EC64C3E8F49CAF5B1F328F55449DB`，备份为 `dist/Peach/Peach.pre-0.7.0-final-20260828-110113.exe`；22 个迁移且 0 待处理，生产健康检查为 writer、非只读，严格 HTTPS 浏览器可打开 `/follow`。真实 ledger 写入前备份为 `database/ledger.pre-follow-paheal-20260828-104302.db`（SHA-256 `423B470B5609FF08DB29D223114C1ED7066DABA2D86DD4ABC96D05D58F2A65DC`）；来源 13→14、条目 713→737，完整性正常、外键违规 0。重复添加大小写不同的 Paheal 链接仍复用来源 15 且新增 0；目标 7428820 与已停用 SubscribeStar 2639932 及同批媒体共享 `subscribestar:2639932`，去重键已在真实库和 API 验证。生产页面显示 Rule34 Paheal 来源、InitialA 作者筛选、右侧 8 视频详情队列和标题旁来源外链；手机视口本轮浏览器读取超时，视觉验收未取得。
- `8b7255c` 已合入 `master`：管理页新增 FANBOX Cookie 密码输入，Cookie 只发给 `api.fanbox.cc`，不会继承到 Gofile。Gofile 现场验证为 token 有效（`/accounts/getid` HTTP 200），但 `/contents/OS2Qz9` 返回 HTTP 401、`error-notPremium`；界面和检查结果改为按 Premium 套餐限制报告，不再误报 token 无效。关注域 382 项、正式 Windows 全量 1110 项通过，分别有 1/13 项按平台跳过。
- 0.7.1 正式 EXE 已替换为 SHA-256 `F3AB422B9004AA5AF4ACDE3BEF055E19D4D88D1E7BCEB5EDA478F39C687E6A38`，上版备份为 `dist/Peach/Peach.pre-0.7.1-20260828-131711.exe`；80/443 已恢复，打包迁移为 22 个、0 待处理，项目 CA 严格 HTTPS `/healthz` 返回 200、writer。生产凭据 API 显示 FANBOX `cookie` 可选输入、Gofile token 已配置。内置浏览器两次加载 `/follow-manage` 均超时，视觉验收未取得；本轮没有触发检查更新或写 ledger。
- `b0730af` 已经由 `a0a8ccc` 合入 `master`：0.7.2 将 FANBOX `post.info` 收窄到固定版本 `curl_cffi` 的 Firefox 传输；列表及其他来源仍走共用 HTTPX，不执行网页脚本、不求解机器人质询、不读取付费帖子。用管理页已保存的 Cookie 对 LazyProcrastinator 帖子 12228983 做只读复核，已取得 6 图、正文和 Gofile `OS2Qz9` 资源页，证明 Cookie 没填错，先前的 403／`general_error` 来自传输指纹。关注域 386 项、正式 Windows 全量 1114 项通过，分别有 1/13 项按平台跳过。
- 0.7.2 正式 EXE 已替换为 SHA-256 `F5F78CDACD9F466A6B8A423151F510F16D40A47BB052B984971D0F59D3EC51E6`，上版备份为 `dist/Peach/Peach.pre-0.7.2-20260828-142052.exe`；80/443 已恢复，打包迁移为 22 个、0 待处理，项目 CA 严格 HTTPS `/healthz` 返回 200、0.7.2、writer。生产凭据 API 显示 FANBOX Cookie 已配置、Gofile token 当前未配置；本轮没有触发检查更新，部署前后 ledger SHA-256 均为 `C2C6F09522305F682636379F43E74A4E60D9475C39FBE0FBC3505A7879CB963C`。本改动没有页面、CSS 或交互变化，桌面/手机视觉验收不适用。
- 2026-08-28 已完成全仓自研实现复用审计：浏览器历史、批处理 PID 锁、Rule34Video 媒体页和 FANBOX 正文模型有成熟替代或参考实现，进入替换队列；MP4 有界索引、固定项目 CA、SQLite 迁移、Gofile、系统网络监听、流媒体会话、单 writer 同步与 Windows 更新因已验证的 Peach 约束继续保留。详细候选、版本、真实 POC 与拒绝理由写入 `docs/REUSE.md`；本轮只改规则与文档，没有新增运行时依赖、部署、生产或 ledger 写入。
- 0.7.3 首项复用替换已完成：FANBOX 正文规范化固定参考 PixivUtil2 `v20251112` / `e537e96`，覆盖 image/text/file/article/video/entry、正文 blocks、四类 map 与旧 HTML；图片和视频进入现有多媒体切换，其他文件保留资源链接，URL 稳定去重。已保存 Cookie 对公开帖 12228983 的只读实测为 article、6 图、Gofile `OS2Qz9`；follow 391 项、正式 Windows 全量 1119 项通过，分别有 1/13 项按平台跳过。
- 0.7.3 正式 EXE 已替换为 SHA-256 `97EBAEE965794D100FA3BC7093B042EB36F1147CDEE1432C3504D7ECFB9D4582`，上版备份为 `dist/Peach/Peach.pre-0.7.3-20260828-152459.exe`；80/443 已由新进程恢复监听，打包迁移为 22 个、0 待处理，项目 CA 严格 HTTPS `/healthz` 返回 200、0.7.3、writer，`/follow` 返回 200。部署没有触发生产检查更新，部署前后 ledger SHA-256 均为 `BC98556370E285C2B627DBA3A36820E79AFF4599A33A265B0DBDDE520379A697`；本改动没有页面、CSS 或交互变化，桌面/手机视觉验收不适用。
- 0.7.4 追更详情的多图改为左右按钮、方向键和点状分页；多视频继续使用 Mix 右侧队列。直达详情按条目 ID 精确读取，启动时不再先绘制首页。关注入口统一使用 RSS 图标，左侧窄栏与筛选抽屉共用可在设置中调整的顺序。自动追更固定复用 APScheduler 3.11.3，只在 writer 启动，默认每小时、首次等待完整间隔，手动与自动检查共用互斥锁；设置页可关闭或调整为 15 分钟至每天，并显示最近/下次运行状态。正式全量 1130 项先发现 1 条点状分页圆角门槛，修正后覆盖该门槛的 `catalog` 368 项全部通过；全量其余 1129 项通过、13 项按平台跳过。
- 0.7.4 正式 EXE 已替换为 SHA-256 `027C985CD85B509BDEAD52D3FB209CBA25421B674BED96E0A97D97C82C8B2C1C`，上版备份为 `dist/Peach/Peach.pre-0.7.4-20260828-160731.exe`；80/443 已恢复，打包迁移为 22 个、0 待处理，项目 CA 严格 HTTPS `/healthz` 返回 200、0.7.4、writer。生产 `/follow/item/791` 返回 200，精确 API 恢复到 FANBOX 条目 791 及 6 张图片；自动更新已启用为每小时，下次运行时间可读且首次未立即执行。浏览器实测左右按钮、左右键、6 个圆点、刷新后保留详情、RSS 导航、设置中的侧栏排序与自动更新状态均正常，控制台无错误；侧栏顺序测试后已恢复原顺序。本轮没有手动触发关注检查。`migrate status` 在 16:05 将 WAL 检查点写回主库，主文件 SHA-256 由部署前快照值变为 `B1D499D5AA89D2BCB7A7311B167C726B07EA4FF0620E1FB467FC3F03519C487A`，部署重启后保持稳定；SQLite `integrity_check=ok`、外键违规 0，asset 81555、follow_item 1008。
- `f41b2c0` 已合入 `master`：F95 每个带文件站链接或非图片附件的回复独立成一项，同一回复只有解析出多个视频才进入 Mix；纯讨论与 GIF／meme 图片回复在连接器和历史读取层隐藏，不删除 ledger。InitialA 真实最新页只读 POC 为 3 条资源回复、6 条讨论跳过，已保存 Cookie 能解开链接；生产历史 17 条只显示 4 条资源回复且各自 0 个变体。管理关注、设置层与普通关闭按钮按 2026-08-28 复测的 Geist 4／6px 控件几何收敛，Tag 与媒体空间导航保留各自语义；`design.md` 锁定 SHA 未变化。最新 master 正式全量 1164 项通过、13 项按平台跳过。生产重启后严格 HTTPS 为 0.7.6、writer，`/follow` 与 `/follow/item/791` 均为 200，F95 Cookie 存在，自动更新仍为每小时且未手动触发；部署前后 ledger SHA-256 均为 `A03E3B450B2721B7FB8078D4A3751C498750B1A8608C66F49CCBD96AE949BF6F`。生产浏览器的 DOM 与截图读取连续超时，桌面／手机视觉验收未取得，不能以 API 和静态测试替代。
- 管理页 `/taste` 以浏览器记录为主、Peach 内部行为为辅，分区展示各自排序；创作者与女优显示实体头像，常访问网站显示 favicon。宽屏改为按信息密度分配 4／6／8／12 栏且面板按自身高度收口，不再固定等宽双列；返回口味页会清除临时下钻筛选。明确“不合口味”只归于具体项目与理由，不自动给 Tag 降权；模糊时长旧 Tag 从口味结果和前端筛选状态排除。正式 Windows 全量 1142 项通过、13 项按平台跳过；1440×900 实测网站 favicon、创作者／女优头像与非等宽面板正常，390×844 为单列且无横向溢出，Tag 下钻后返回口味再进首页不残留筛选。原始 URL／标题仍不进入页面或 ledger，本轮未改真实 ledger 中的旧实体行，也未重启 80／443 生产服务。
- `/taste` 的浏览器查询词重新按语义解析：`-gay`、`-ai_generated` 这类排除项整体不计入画像，下划线保留为组合词，`detroit`／`become_human` 归入游戏内容；本机实数复核后仅剩 2 次显式正向 `ai` 页面，`gay`、`generated`、`ai_generated` 均为 0。SimpCity、Hanime1 与 Kemono 优先使用站点实际 favicon，失败才回退通用图标服务。`/taste` 与 `/stats` 使用信息密度 Bento；任务队列继续保留线性列表。管理导航在 760px 以下默认折叠为当前页单行，展开后为两列 8 个入口；390×844 实测收起／展开、当前页高亮及无横向溢出均正常。Mac 的 Safari／Zen／Firefox／Chrome 本机发现已有测试，但当前 Windows 私有历史库只有 LMD-DST 的 7 个 Chrome／Firefox／Zen profile、382782 次访问，不自动跨主机读取。正式 Windows 全量 1154 项通过、13 项按平台跳过。本轮为预览源码，未部署或重启 80／443。
- `/taste` 排名按 Vercel Geist Grid 的共享引导线重新收敛：普通排行与数据源由父网格统一拥有边线，单元透明、0 圆角，排名按 DOM 阅读顺序逐行排列；桌面紧凑／半宽／宽／满宽面板分别为 1／2／3／4 列，中等视口统一 2 列，窄屏统一 1 列。Tag 保留 6px 小圆角，不再使用全胶囊；hover 只改变 5% 轻底色，取消蓝字、粗左边、阴影和整块圆角浮层。顶部操作继续左对齐，头像、favicon、固定数字列和最右操作列不变。1137px 实测各面板列宽固定、无横向溢出；390×844 下所有排行和数据源均为约 313px 单列，页面宽 380／390。catalog 387 项通过，浏览器控制台 0 个错误。本轮只更新 8900 预览，生产 80／443 未重启。

## 下一批工作

1. 另行授权后先备份 ledger，修正 4 组已核实姓名：恢复 `星谷瞳`、`福山美佳`、`平沢すず` 的规范名；从 `かわいゆい` 移除错误的 `河合ゆい` 别名与 r18 外部引用，清退错误头像及 provenance 后重新生成候选；同步 actor tag/检索投影，并核对受影响行数、`integrity_check`、外键和只读 API。
2. 另行授权后先备份 ledger，再把 `follow_item` 181、184、185 从 `seen` 恢复为 `new`，复核状态计数、完整性与新哈希。
3. 继续按复用审计依次替换 PID 锁和 Rule34Video 媒体页；每项固定版本/revision、首个消费者和隔离测试同批落地。
4. 分类剩余 44 个无预览变体，区分确无图片与来源解析遗漏。
5. 另行确认后在生产关注页检查 LazyProcrastinator FANBOX，把已验证的 6 图、正文与 Gofile `OS2Qz9` 资源页写入关注候选；Gofile token 当前未配置，且此前验证的账户不是 Premium，21 个视频仍未取得。
6. 在 `/review` 人工处理本批 JAV 日文系列名、官方标签及现有创作者标签、FC2、Javinizer、Logo、头像和媒体失败候选；未经批准不写真相字段。
7. 将 Windows writer 的最新副本同步到共享传输点，再让 Mac reader 拉取；同步前后核对迁移版本、计数、完整性与 writer 身份。
8. 在 Mac Finder 以 `smb://peach-win.local/peach-sync` 连接一次并保存钥匙串记录，再重启菜单栏进程，核对自动挂载、reader 锁定、HTTPS、mDNS 和真实 LAN 客户端。
9. 在实现下载器前先确定媒体凭据、流量与磁盘预算。
10. Windows writer 运行 PikPak 夜跑前重算 probe/抽帧队列，并按 `peach-batch-jobs` 设置流量与系统盘闸门。
11. 继续拆分 `web_contract.py` 的 catalog、stats、activity、review、trash 领域，保持路由和现有契约测试不变。
12. 补做 HLS 首帧、seek、自适应码率及桌面/手机视觉验收。
13. 外置盘挂载后先只读盘点 `R:\Media\<名字>\P\...` 图片规模；扫描写真 ledger，需另行授权。
14. 重做品味分析页的视觉再决定是否合入：`agent/codex/taste-analysis`（cd3effe）功能可用但版式不过关，2026-08-29 集成时按用户判断跳过。重做时以该分支的 `taste_history.py` 分析逻辑为底，版式另出。
15. 决定 `attic/instances/20260828-taste-preview` 的去留：8900 预览实例的数据根，含 122 MB 账本副本（2026-08-28 18:08 快照）与 153 MB `sources`，`generated` 已空。账本副本按真相源快照对待，删除需另行确认；28 个预览日志（348 KB）可随时清。

## 批处理进度（自动生成）

<!-- job-status:start -->

<!-- 由 scripts/job_status.py 生成，勿手改；数字现算于账本与产物 -->
<!-- generated 2026-09-02T15:28+08:00 -->

- 最近自动交接：`claude` / `Stop` / `completed`，2026-09-02T15:28:04+08:00。
- 资产 75500 条，其中视频 22509 条。
- 待抽帧（可抽 / 缺时长待 probe / 合计）：
  - `local`：3 / 1 / 4
  - `115`：10 / 129 / 139
  - `pikpak`：8881 / 1310 / 10191
  PikPak 的策略组已可切 DIRECT：2026-08-15 实测走代理时 9 帧 163 MB / 13.7 秒，走直连时 30.5 MB / 64.2 秒——慢约 4.7 倍但流量少约 5 倍且不占代理预算。全量抽帧仍是 773 GB 量级（代理口径），按创作者采样 88 板直连约 2.7 GB。115 一直走直连，同样动作约 285 MB 一张接触表。
- 无内容标签视频 6348 条（占视频 28%）。
- `asset_tag` 来源分布：`vision_creator` 23823、`pixiv_tag` 19753、`name` 17202、`stash` 12858、`javinizer:r18dev:tag` 2060、`performer` 1589、`follow` 1376、`r18:performer` 1122、`javinizer:mgstage:tag` 964、`javinizer:fc2:tag` 950、`r18` 522、`javinizer:aventertainment:tag` 106、`javinizer:libredmm:tag` 43、`javbus:performer` 37、`javinizer:r18dev:performer` 33、`javinizer:caribbeancom:tag` 32、`javinizer:dmm:tag` 25。
- 番号 1456 个，其中 1201 个有厂牌（82%）。

| 产物 | 行数 | 生成时间（本地） | 说明 |
| --- | ---: | --- | --- |
| `code-scrape.csv` | 1135 | 08-22 10:39 | 番号刮削结果 |
| `name-clean.csv` | 0 | 08-30 20:28 | 文件名净化清单 |
| `ad-candidates.csv` | 82 | 08-14 22:53 | 广告候选（confidence 分级） |
| `disposal-candidates.csv` | 54 | 08-14 22:53 | 待处置候选（含「保留」判定） |
| `creator-tags-review.csv` | 86 | 08-15 18:35 | 创作者标签待审 |

<!-- job-status:end -->
