# 悬停放大的快退／快进控件：用户截图证据与 beeg 侧未取得

- 取证日期：2026-09-04
- 用户截图：黑底上一对细描边圆形箭头（左逆时针、右顺时针），无底色、无边框、无数字角标。
  原图未落盘，无法给出 SHA-256；要复核只能再要一张截图。
- **不登记进 `docs/reference-sources.json`**：那张表要求每个来源有可重新抓取、可哈希的上游
  快照。用户截图没有上游 URL；下面 beeg 那一侧是可重抓的，但结论是「该控件在上游不存在」，
  登记一个没有对应表面的来源只会在下次 `accept` 时被当作漂移。

## beeg 侧：未取得

用户要求「使用 beeg 的这种」。按 `peach-reference-evidence` 先取源码：

- <https://beeg.com/> 当前入口指向 `/dist/main.8fa6fc40.js` 与 `/dist/main.8fa6fc40.css`。
- `main.8fa6fc40.js`，758,486 字节，SHA-256
  `eb321bed124d50b59f708eef2a8ae9874a22b23a7b266f548e5f82777b462fc4`
- `main.8fa6fc40.css`，359,426 字节，SHA-256
  `a30639614190e9d00e2aef0bb06d102c51a1d60886e3460f0947b0500552ddf8`
- 该 bundle 无懒加载分块映射，整站一份 JS。全量检索 `seek`、`rewind`、`forward`、`skip`、
  `jump`、`backward`、`10s`、`replay` 无任何快退／快进标识；`Tile__startPreview` 一路只做
  预览播放，不挂控件。图标组件里唯一的圆形箭头是 `ArrowClockwiseIcon`（16×16 viewBox、
  `fill="currentColor"` 的实心路径），全 bundle 只有定义、零处引用。
- 结论：**beeg 当前线上界面没有快退／快进控件**，截图里那一对箭头无法由 beeg 的字节重放。
  这一侧记为未取得，不据此声称忠实复刻。

## 可复用证据（只来自用户截图）

- 快退／快进／全屏这一组居中控件不画按钮的底色、边框和磨砂层，只留描边图标。
- 箭头为细描边（视觉上约 1.5px），左侧逆时针、右侧顺时针，成对等大。
- 图标上不压秒数数字。
- 截图只锁定这四条，不锁定字号、具体像素尺寸、间距、投影和悬停动效。

## Peach 采用与差异

- **「不画底色、边框和磨砂层」这一条不采用**。2026-09-05 用户当场指定：这三颗与右下角
  「稍后看」同款，只是尺寸大一号，要有背景和框。用户的界面决定优先于这份截图证据，
  这里只记录取舍，不据截图回推。
- `.hovertools.seektools button` 只覆盖图标尺寸（34px）。底色、边框、磨砂和悬停填充全部
  由 `.hovertools button` 共用规则给出，命中区域仍是 58px 圆。58／34 与「稍后看」的
  36／21 是同一个比例，一排控件因此读成一组。
- 秒数由 `title` 与 `aria-label` 承担（「后退 10 秒」），DOM 里没有数字角标。秒数本身仍
  可在设置里改。
- 图标用本地 Lucide 的 `rotate-ccw`／`rotate-cw`，不复制 beeg 的 SVG 路径。
- 同组第三颗「打开详情」沿用同一外观，一排三颗不混两种写法。
- 右下角「稍后看」按钮的磨砂圆底不变——它是持久状态而不是瞬时控件，`aria-pressed`
  的开态需要一块面来承载。
