# 搜索下拉的分类页签与人像行：用户截图证据

- 取证日期：2026-09-26
- 用户截图：深色下拉栏，顶端一排英文页签（People、Brands、Other 等），每行左侧一枚圆形
  头像，名字下面一行描述，右侧一个计数。
- 来源站点：**未取得**。截图里没有站名、URL 或可辨认的品牌标识。
- 原图未落盘：背景里有露骨缩略图，而 `peach-app` 同步到 GitHub，所以不进仓库，也就给不出
  SHA-256。要复核只能再要一张截图。
- **不登记进 `docs/reference-sources.json`**：那张表要求来源有可重新抓取、可哈希的上游快照，
  这张截图没有上游 URL。

## 可复用证据（只来自用户截图）

- 补全按种类分页签，页签排在下拉栏最上面。
- 人这一类每行是「圆形头像 + 名字 + 一行描述」，一眼认得出是谁。
- 截图只锁定这两条结构，不锁定像素尺寸、字号、颜色和间距。

## Peach 采用与差异

用户原话：「不用照抄，因为我们宽度不一致，窄屏下可以参考，你来自己发挥一下」。

- 窄屏一栏，人名行照截图的形态：40px 圆形头像、名字、一行「N 个视频 · 事务所」。
- 宽屏按下拉栏自己的宽度分栏（容器查询，≥720px 两栏）：左栏是身份和词，右栏是视频封面格。
  一栏宽到 420px 时人名行右侧摆四部近作的小封面，点封面直接开详情。
- 截图里右侧那个计数挪进副行，右侧位置留给近作封面。
- 页签多一枚「全部」，各类页签带这段输入在该类的命中总数，用 Board 的计数徽标
  （`.board-tab-count`）；样式照复核分类那排药丸：选中只有填充，未选中悬停只提字色。
- 视频、女优、创作者、厂牌、事务所、系列、标签各是一类，只列有命中的那几类；
  只有一类命中时不出页签。
- 点人、公司和系列进资料页，点标签照旧按词搜索，点视频开详情。
- 尺寸取自 Peach 自己的写法：公司标识 28px 圆片（与厂牌药丸同形），小封面沿用卡片的
  3:4 容器与正封取景。
