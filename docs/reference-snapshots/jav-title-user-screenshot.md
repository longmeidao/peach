# JAV 标题用户截图证据

- 取证日期：2026-08-31
- 来源：用户提供的标题截图；原页面 URL 与 DOM 未取得
- 原图：861×111 PNG
- SHA-256：`7bebe5a006fac1b96be147ff29fd13ded779ca2897284e0580ecae7a7bb9b91d`

## 可复用证据

- 标题先显示大写番号，番号使用粗体；后接作品标题正文。
- 可见标题不显示 `.mp4` 等媒体文件扩展名。
- 截图只锁定标题语义与层级，不锁定字体、字号、颜色、截断宽度或页面间距。

## Peach 主动保留的差异

- 文件定位、播放统计与资源管理继续使用 API 返回的真实文件名，不修改 ledger 或磁盘文件。
- 只有 `is_jav` 为真的项目使用这套显示；普通视频、图片、压缩包和管理页文件名保留扩展名。
- 账本只有“番号.mp4”时只显示番号，不凭截图伪造官方标题；已有完整标题时才显示“番号 + 标题”。
