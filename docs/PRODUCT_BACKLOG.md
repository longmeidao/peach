# Peach 产品待办

更新时间：2026-08-25。这里只记录尚未完成或只完成一部分的需求；运行数字仍以 `docs/STATUS.md` 自动区块为准。

## 已有骨架、尚未完成（4 项）

1. **Mix / 合集 / 播放列表**：自动 Mix 已混排进普通视频流，点击后使用独立 `/mix/{seed}/{item}` 路由并在播放器右侧显示当前队列；仍缺可命名、排序、保存、续播、编辑的持久播放列表，以及保存自动 Mix 的能力。
2. **寻找更好版本**：已经能逐条标记「高清 / 无水印 / 完整版」等目标；后续仍需相似内容匹配、候选去重、来源发现和人工替换确认。
3. **现代自适应播放**：Video.js、Range、统计面板和面向 115/PikPak 原生 MP4 的按需 HLS 清单已经上线；自适应码率、多路清单、快速首帧和来源层大块预取优化仍未完成。
4. **在线追更**：已有 RSS/Atom adapter、条件请求和快照存储；真实订阅源、调度、审核写入和 UI 尚未接通。

## 尚未实现（8 项）

1. AI Provider 的真实调用、能力协商、Credential Manager 凭据和候选审核 UI。
2. 剩余厂牌 Logo 补齐与持续校验。
3. 剩余单一创作者风格板复核、无标签内容补标。
4. 开头/结尾额外抽帧、片尾出处 OCR、「Full version」不完整候选。
5. 缺时长资源补 probe 后再生成接触表。
6. PikPak 计费抽样与下载边缘质量核验。
7. Stash CommunityScrapers 的协议复用与元数据导入。
8. 剩余 status / suggest / ledger 逻辑移入应用边界后删除旧 CLI 表面。

合计：**12 项开放需求**，其中 4 项已有骨架，8 项尚未实现。头像人脸取景（2026-08-22 上线，见 STATUS）和 115 大小写路径失败（2026-08-17/18 收尾，2 条失败已定因）已完成，不再计入。本轮设置、5 分钟刷新、搜索历史、播放器统计、动态环境光和 Shorts 风格沉浸模式不再计入开放需求。
