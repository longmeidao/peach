---
name: peach-surfaces
description: 在改完界面、API、契约、数据层或用户可见文案，准备收尾、报告完成、验收或提交前使用；用户说漏了、别的页面没改、只改了一半时也用。
---

# 每个表面都要过一遍

最后复核：2026-08-17
证据来源：`docs/HANDOFF.md` 各次事故记录、`AGENTS.md` 语义契约门槛、ADR-0015。

本仓库最常见的缺陷是「改动在你测试的那条路径上成立，在别的表面缺失」。收尾前逐项声明
**适用并已处理** 或 **不适用及原因**，不要跳过不适用的项。

## 清单

1. **数据层**：ledger 查询、迁移、`entity` 与兼容投影是否需要同步？只改显示文本而数据层未动，
   多半是漏了。
2. **API 契约**：`/api/*` 的请求参数、返回字段、404 行为；写入端点是否有对应的读端点。
3. **页面表面**：首页、详情、沉浸、实体资料页（女优/厂牌/创作者/系列）、`/tags`、`/stats`、
   `/trash`、`/review`、`/mix`。一个交互改了首页卡片，通常也要改实体页卡片。
4. **反向状态**：加了「进入某状态」就必须有「退出该状态」。稍后看/已标记/回收站/隐藏标签
   都成对存在。
5. **视口**：桌面默认视口与 390×844 手机各看一遍，横向溢出与文字折行分开报告。
6. **测试**：数据层测试 + 页面源测试。语义契约（UI 标签、身份、反馈状态、搜索推荐）两者都要有。
7. **文档**：`README.md`（对用户）、`docs/STATUS.md`（运行态与下一步）、`docs/HANDOFF.md`
   （长期规则与事故证据）、ADR（架构取舍）。不要把内部实现细节写进面向用户的段落。
8. **两个智能体入口**：新规则或新流程是否需要进 `AGENTS.md` 索引或某个技能，Codex 才能读到。
9. **生产**：服务是否已真的切换并复核端口与进程归属。没重启就不能说已上线。

## 验证分开报告

静态/单元/API、桌面浏览器、390×844 手机、生产是否已重启，四项分别给结论。
任一项未做就写「未验证」，不得用其中一项冒充另一项。
