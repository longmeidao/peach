# 变更日志

本文件记录 Peach 每个发布版本里值得使用者知道的变化。

格式遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。分组标题是
「新增 / 变更 / 弃用 / 移除 / 修复 / 安全」，对应规范的 Added / Changed / Deprecated /
Removed / Fixed / Security。

每个条目前面带一个区域标签：分组标题按变化性质分，标签按使用者看到的区域分，
取值限于「界面 / 播放 / 馆藏 / 采集 / 复核 / 关注 / 配置 / 更新 / 桌面 / 账本」。

版本号只在发布点推进：每个 `X.Y.Z` 都对应一份可下载的制品，主线上的其它提交由 commit
标识（ADR-0012）。`## [未发布]` 收的是已经进了主线、还没发出去的变化。

本文件只收有制品的版本。`0.7.0`–`0.7.2` 早于发布工作流，`0.8`–`0.29` 区间里的版本号由
每次本地集成推进产生，两段都没有对应制品，逐条变化见 Git 历史。

条目草稿由 `python scripts/changelog.py` 从提交主题生成，措辞由人改成使用者读得懂的话；
只改开发过程的提交（文档、测试、重构、构建）不进本文件。

## [未发布]

## [0.33.0] - 2026-09-14

### 新增

- **关注**：关注图片墙的宽高统一成一个字段，三条路补齐
- web：数据管理页来源标官方站标，操作键统一主按钮，忙态各自适配
- **关注**：回抓支持 rewind 从头重走
- **关注**：fanbox 媒体带固有宽高，存量行重探补齐
- **关注**：fanbox 卡面封面优先、Poll 帖剔首位图表、媒体加载失败给说明
- web：详情开场先挂本地封面海报位
- **关注**：fanbox 支持全量回抓并补收存量封面
- **关注**：关注条目支持按图隐藏与恢复
- **关注**：fanbox 正文媒体补收封面图
- web：点详情浮窗外面就退出
- web：关注页「检查更新」进页头当主按钮，「换一批」真正打散整批，「仅显示图片」改图标开关
- web：产地筛选项、详情入口与批量判定
- **馆藏**：产地独立成维度，JAV 归口为它的投影
- **关注**：调整创作者别名表单与首次采集范围设置
- web：进度条按时间点显示画面，设置里选采集密度
- **播放**：按时间点预抽视频缩略图并经接口下发
- web：整理作者别名表格与扫描采集菜单
- web：没设访问密码时，卡片里常驻一条警示
- **馆藏**：扫描与采集可以分开跑
- **采集**：图库里只命中一张的女优头像批量装上，其余产对照表
- **关注**：建议分出 rule34.xxx 的作者，等站点时下拉自报在做事
- **关注**：添加框敲字出建议，本机清单与站上标签分两组给
- **采集**：javdb 别名落库按批次署名，整批撤得回
- **采集**：javdb 采集同趟产出中文别名候选，落库另立脚本
- **关注**：本库挑不出明显头像时去站点找题材圆标
- web：关注页浮层对齐首页并按列排序，名册页加在线一档
- **关注**：题材行按本次访问的种子随机取，进一次换一批
- **关注**：题材圆标收成首页那种小圆标并按检出的人脸取景
- **关注**：题材归成系列并配上本库里最热的那张代表图
- web：关注页顶上补题材那一排，状态收到四枚
- web：索引页与标签字母表按 Board 重排
- web：资料页与关注页按 Board 重排
- web：别名支持自己输入
- web：大图卡片只显示书脊折痕右侧的正封，两侧留白垫模糊背景
- web：资料页可自己换头像
- web：大图版式按 poster_box 取正封
- web：采集页与口味页的站点图标改由本机取
- **馆藏**：番号解析认 Tokyo-Hot 与西片日期，噪声按三份名单剥
- web：活动页把任务中心的三段摊在一屏
- web：任务中心接进后台任务、批处理与命令行
- data：任务中心的 task_run 表与服务层
- web：批准钉死单条资产时带上 expected_revision
- **复核**：真相字段写入改走归属入口，批准支持乐观并发
- **账本**：真相字段记字段级归属与写入序号
- **采集**：鉴权失败单列一档，撞墙的来源本批不再问
- **馆藏**：外挂字幕落库、转 WebVTT 并挂进播放器
- web：社媒标记取各家品牌色，常见七家一律走图标库
- **采集**：JAV 横版封面的 2:3 竖海报取景框
- **采集**：头像站点水印的检出与移除
- web：筛选条三格两页共用，窄屏只钉住最左那一档
- web：厂牌标识随仓库分发，取图回落到内置资源
- **复核**：复核卡上就地收录未收录 genre，候选改成 Radio card
- web：重设计数据管理子页面
- web：首次设置页改用 Auth Card 版式与勾线动效
- tooling：高置信广告批量入回收站与稀疏识别筛选
- **采集**：无番号视频的联网识别清单与候选合并
- **采集**：无番号视频用同目录图片作本地海报
- **馆藏**：广告包的形态证据进入垃圾复核队列
- **采集**：从 MIB 官网采集作品元数据、封面、演员与厂牌标识
- web：换筛选回到新名单的开头，生效的那几条按 Chip 画
- web：侧栏名单末尾那枚键收的是整组，位置归人自己管
- web：侧栏名单没列完的话，那句话接在名单末尾
- web：顶上三排横滚到右端接着续下一批
- web：按下去缩一档，松手按弹簧弹回来
- web：首页那几排从侧栏玻璃底下穿过去，左边界不动
- web：筛选条左边那四枚钉住，横滚只留给右边的标签
- web：那块玻璃换成一条弹簧，侧栏也照着滑
- web：玻璃薄一档，面上两团光在极慢地挪
- web：玻璃的落影分三层，选中那块弹出它所在的那一行
- web：首页玻璃按亮度读数并让视图共用一块滑板
- web：取不到的链接可以勾选重试
- web：统一数据管理工作区
- **关注**：增加未看时间范围
- web：用分段控件切换关注工作区
- web：重设计关注管理页面
- web：配置进设置弹层，资料页浮层与采集进度补齐
- 配置面板提供自动更新设置
- 厂牌矢量标识归一成带边距的方图
- web：搜索栏边打边补出馆藏里的身份与作品
- **界面**：复核队列分页、数据管理页读数卡、首页两排改竖排人像与大 Pill
- **关注**：跨作者合集只数画面作者，阈值收到 3
- web：下拉菜单统一 boardui 开合动效，Toast 退场按 Notification 收口
- **关注**：关注列表改成默认视图与表格视图
- **界面**：补齐 Board 控件与历史时间分析
- **界面**：统一媒体库图标与列表视觉规范
- **界面**：完善侧栏媒体库与主题交互
- **界面**：统一导航排版与统计口味卡片
- **界面**：迁移 Board 界面与设置控件
- **关注**：作者头像与来源图标落在本机，刷新周期进设置
- **关注**：关注页作者与来源筛选多选，三行各带全选与全不选
- release：Release 页正文取自 CHANGELOG.md 对应那一节，缺节不发

### 变更

- **采集**：来源说「没有」的番号 7 天内不再问，无番号视频不再列为问题项
- hls：分片重编码走 NVENC 链，同一分片只起一个 FFmpeg，Range 按 1 MiB 读
- stream：不兼容片源没有账本时长也按片切，不整片转码
- **播放**：Windows 账本路径只做 abspath，不再 resolve
- **采集**：FC2 番号不再问 r18.dev 要资料，只问封面
- **采集**：只采集跳过齐全的行，同目录只列一次，落盘节流
- scan：扫描按目录列表自带的大小与时间登记文件
- **关注**：站点不通时联想与圆标补全立刻回空
- web：悬停扫格子先把下一格取回来再换上去
- **配置**：开机启动项状态按快捷方式指纹留存并在服务起来时预热
- web：明暗切换那圈扩散换成渐变遮罩

### 修复

- tooling：两个工作树落点共用同一条残留判据
- web：窄屏搜索开合的聚焦不带着页面滚
- **播放**：人脸检出跨三个尺度取中位数，圆标不再框在躯干上
- **关注**：尺寸回填先问缩略图，文件头多认 AVIF
- web：关注图片墙瀑布流不再随图片加载重排
- **关注**：标题日期批次标签不再冒充版本
- web：关注页控件对齐首页并修正图片墙骨架
- stream：客户端断开后 Range 响应停止读文件
- web：完善关注与复核工具栏及采集反馈
- web：详情浮窗补上退场，来回不再重画顶部三层
- web：关注页题材行收人物、按词提首字母、按形态剔公司
- web：完善关注分页、别名多选与响应式操作
- web：复核骨架玻璃跟随容器且管理 tabs 保持全宽
- core：清退账本备份时扫掉无主的 -wal/-shm
- web：复核工具条始终共用玻璃并将分类放到右侧
- web：稳定关注悬停并统一详情浮窗
- web：统一图片大小与瀑布流切换并修复关注卡片封面
- **界面**：修正管理页面占位与设置入口
- **界面**：按页面结构呈现加载骨架
- scripts：九宫格批处理收尾只等四分之一秒
- web：配置卡的正文不再被裁在卡外，文件夹行两枚按钮和输入框同高
- **馆藏**：同番号下文件名一样的两份算同一条重复
- **关注**：合辑按同一份画面作者计数挡住，历史条目读取时回算
- **采集**：补头像脚本声明 pid 锁参数，对照表链接按本机端口拼
- web：全站头像按检出的人脸取景，不再几何居中
- **关注**：圆标的脸还要够清楚，一张都不够格就摆全身
- **关注**：在线名册按整行取官方头像
- **关注**：题材圆标挑到脸够大的那张候选
- **关注**：圆标落盘按各自需要的边长，先裁掉源图自己留的黑边
- web：脸摆不正也算放大的理由，放大的上限改问构图不问倍数
- web：小圆标里的脸按像素下限放大，落盘那份留够放大的余量
- web：资料页切换回到筛选条圆键，筛选药丸未生效一律虚线
- **采集**：书脊两条边都在窗里时按正封形状认折痕
- **关注**：按 F95 的标题约定与首楼名片认作者名和头像
- web：头像候选按整条名字链取并集
- web：卸载区读成危险区，危险档的红并到 BoardUI 的一副面上
- web：主按钮照 BoardUI 的三档渐变，悬停那一层铺在伪元素上淡入
- web：设置弹层右侧连成一张底，标题的影子等留白滚完才出现
- covers：切点走到折痕斜坡尽头，正封左缘不再压着书脊
- covers：书脊折痕按正封形状认，边车里的框就是正封本身
- web：设置弹层左栏去掉顶边高光，玻璃跟着指针走
- web：按钮那一面收归一处，强调档压上去提亮一档
- web：按反馈修界面十处
- web：管理页正文对齐标题，「这台电脑」等回话再列出
- **采集**：番号闸门保住 Tokyo-Hot 的小写写法
- web：网盘图标统一站标，媒体库选中改用同一块玻璃
- web：关注列表工具行放不下时收成图标
- web：账本只读那一条改用 Note，图标与文字跟着色调走
- web：任务中心的第一份进度、顶替收尾与同步等待
- web：采集检查的裸 403 不再让用户去登录
- **采集**：只凭状态码判出的 403 不替用户断成因
- http：响应文本按站点声明的字符集解码
- **采集**：番号闸门与重命名认得日期式的两种分隔符
- **馆藏**：日期式番号的分隔符进身份，不再折叠
- scripts：演示库生成器打印的设置行用 TOML 字面串写法
- web：稳定搜索与主题切换中的玻璃合成
- web：会横向溢出的层一律登记那套滚轮与渐隐
- web：资料页那排外链窄屏改横滚
- web：外链摊平的豁免只认资料页那枚圆盘
- web：社媒标记回到药丸里，等待态那块浮层是一块
- web：明确封面采集结果并修复侧栏与链接反馈
- web：精简随机入口并调整亮色玻璃与窄屏设置
- web：保留卡片悬停边框与筛选玻璃的溢出动画
- web：恢复播放器独立的深色悬停提示
- web：窄屏筛选框按滚动方向切换吸顶
- web：搜索玻璃从图标位置弹性伸展
- web：修复侧栏高亮与窄屏搜索开合
- **采集**：Gfriends 索引退回旧缓存时告警，未收录不记结论
- tooling：拒绝 --source 与 --swap-from 同用
- web：恢复的数据管理子页重设计接到当前主线
- web：调整数据管理子页浮层与卡片宽度
- web：图片墙秒开、手机上浮层收成两行、资料页那四枚补上滑动玻璃
- web：数据管理页去掉重复入口条，处理失败的问题清单收进折叠
- web：照片墙改等宽网格，各列从顶上开始排
- **采集**：扫描与采集的死任务状态写回中断，界面不再来回跳
- web：资料页浮层合成一整块玻璃，补上交集条，照片栏留下半并能换一批、切大小
- web：整理提示不再跟着每次取数塌一下，该出现时是长出来的
- web：点一条筛选只换底下那份名单，顶上那几排横滚到底接着续
- web：一个标签只有一张脸，加上去的那几枚排在最前面
- web：侧栏那块玻璃对的是落定后的布局，不是画到一半的那一帧
- web：那块玻璃标位置，排序键靠字说话，悬停也交给玻璃
- web：顶栏不铺底，玻璃条上被选中的那一枚哪儿都是同一块白玻璃
- web：选中那块玻璃冲过头多少跟着这一跳的跨度走
- covers：韩国 MIB 不向 JAV 来源要封面，跨作品判据比到数字段
- web：旧任务的问题列表投影进计数，失败时保留发起入口
- tooling：POSIX 入口测试排除 WSL bash 并给探测显式编码
- web：扫描与采集列出失败项并只重试这些项目
- **采集**：扫描与采集任务报告当前项并在预算内继续
- runtime：找源码托盘的窗口类名用 wintypes.LPWSTR
- tooling：署名门槛逐个提交判，不只判分支尖端
- **关注**：查找进度按计划的来源数走满，手柄型 id 不分大小写
- runtime：命令行按 Win32 的三条规则劈，找源码托盘先筛便宜的判据
- web：焦点环收进静止边的内侧，调试 serve 仍要本机才配得了
- web：按 BoardUI 取证收齐输入焦点环与设置弹层滚动条，补源码托盘重启命令
- tooling：署名名单登记 opencode，glm 写的提交能按真名收
- **关注**：查找进度按来源计数，进度环逐个来源真实上涨
- web：关注管理的查找结果与来源行改用主题内可读的层级
- **关注**：发现阶段把手柄对应的非数字创作者 id 也按站内 id 精确命中
- web：数据管理和关注页上几处说明改成读得懂的话
- web：界面只留 Board 一套，设置里的分隔线和滚动条各归各位
- web：未看范围走 Geist Select
- web：恢复关注管理连续分区
- **复核**：候选落库前核验来源自报的番号
- tooling：钩子输出钉住 UTF-8，该发一版了只由钩子说
- web：浮层上下留一样宽的空隙，详情不再钻到它底下
- scripts：外链去重按字节比路径，不再被转义大小写骗过
- web：悬停不再描线，换一批放平，资料页标签与外链改对
- 补底判的是这一趟的产物，不是原图的短边
- 比小圆片还小的厂牌标识用自己的底色补到 64 实像素
- 装着的方标太小的厂牌也算进要找图的名单
- 厂牌标识摆进圆形图位不再发空或被切角
- web：项目横幅的警告字形补齐圆点
- **关注**：关注页筛选行不配全选与全不选键
- web：剩下七处次级按钮补齐那圈 1px 环
- **界面**：图表卡回到卡片面，维度切换换成分段控件，选中 tab 回蓝
- **界面**：选中态改文字色，复核悬浮框收进内容列，卡片上的凹片随主题翻面
- **界面**：复核分类改次级菜单，扫描提示外挂，图例小卡直接是灰片
- **界面**：管理页标题一律对齐 1120，复核悬浮框合成一块面，详情关闭键亮色下黑底
- **界面**：视频网格去外框，Instagram 用品牌圆盘，媒体库菜单不压侧栏
- **界面**：排名展开键与复核悬浮框对齐 Board，批量条隐藏键归位，侧栏切换器改 32px 圆标识
- **界面**：关注列表进框、勾选框画勾、表格框线与可排序列，图标选择器扩到 42 枚
- **界面**：口味、复核、首页与详情控件对齐 Board 规范
- **界面**：媒体库图标选择器只放图标，一行七个
- **界面**：统一 Board 控件与任务状态反馈
- **界面**：校准历史来源与控件状态
- **界面**：对齐导航与按钮并统一关注疏密排版
- **界面**：清理列表样式并校准回归契约
- release：changelog.py --notes 失败时说「Release 正文未生成」

## [0.32.0] - 2026-09-09

### 新增

- **关注**：关注页的作者与来源筛选可以同时选多个，行首各有「全选」「全不选」；标签行加「全不选」（多个标签仍按「同时具备」筛，所以没有「全选」）。
- **关注**：作者头像与来源图标由 Peach 取回保存在本机，页面不再直接向对方站点要图；站点打不开时继续显示已保存的那张。视频与图片仍不保存。
- **配置**：新增「头像与站点图标刷新」，可选每周、每月、每季或从不；作者头像、来源图标和资料页外链圆标都按它重取。
- **采集**：抓取高清封面时页面上写着「正在抓取封面」；离开这一页再回来，还在跑的那一趟接着显示。

### 变更

- **界面**：整套界面换成 Board 风格：顶栏、侧栏和吸顶浮层是半透明玻璃，卡片、按钮、输入框和菜单按同一套尺寸、圆角与间距重画，明暗两个主题各成一套。设置里的「增加对比度」关掉玻璃的透明与折射，改用实色背景。
- **配置**：配置页搬进右上角的设置里，成为「这台电脑」这一块，按「通用 / 媒体 / 网络与访问 / 更新与维护」分开列，管理菜单不再单列「配置」。在别的设备上打开设置仍看得到这一块，里面写明这几项要在运行 Peach 的那台电脑上改。

### 修复

- **界面**：女优、厂牌等资料页顶上那块筛选浮层改成和首页同一块玻璃——标签、计数和排序键的大小、间距、圆角全部对齐，标签一行滚到头时两端渐隐，不吸顶时也不再显出上下两层。
- **复核**：复核页顶上的工具条和分组条从进页面起就是玻璃，两条合成一块，左右和标题、卡片同一条边；上面那排页签也回到同一条左边，选中那块底色不再往左溢出。
- **界面**：首页整理提示和它下面那块筛选浮层之间，留出和别处一样宽的空当。
- **界面**：数据管理页资源面板里的进度环，和它下面那几组链接表对齐。
- **界面**：设置里「增加对比度」和「左侧导航」之间只剩一条分隔线，右端和上面几行齐平；设置打开时页面右缘不再多出一条拖不动的滚动条。
- **界面**：数据管理页和关注页上几处说明改成读得懂的话——链接按「官网/事务所、社交账号、作品资料站、资料出处」分类，检查结果分成「页面已经没了」和「这次没访问成功」两组并写明区别，文件检查说清是拿馆藏里的记录去磁盘和网盘上找文件，关注凭据那一段写明它存成运行 Peach 那台电脑上的一个文件、谁能打开它。

## [0.31.0] - 2026-09-08

### 新增

- **关注**：SimpCity 线程可以作为关注来源。站点不让游客读帖，需要把你自己的登录 Cookie 写进凭据文件；Peach 不解任何机器人质询。
- **关注**：在「添加关注」里输入名字查找时，也会用你的 Cookie 搜 SimpCity 站内线程标题，结果带上版块标签（如 OnlyFans、Simp Chat）供你分辨。
- **关注**：来源筛选菜单新增「全选」与「全不选」。

### 修复

- **关注**：SimpCity 帖子里的网盘链接（Gofile、Pixeldrain 等）现在能从站点的跳转链接里还原出来，详情页会显示对应按钮；帖子里的图片不论挂在站方图床还是哪家第三方图站，都能在详情页就地查看（Peach 代取时不带任何登录态，也不会去取内网地址）；有图的楼层以图为主，网盘链接只做按钮；来源图标补上 SimpCity。

## [0.30.0] - 2026-09-08

### 新增

- **配置**：新增桌面快捷方式开关，可直接创建或移除桌面图标。
- **界面**：可折叠的区块标题统一带上展开箭头。

### 移除

- **关注**：命令行的 `peach follow` 子命令删除；登记、检查、复核与保存全部在关注管理页完成。

### 修复

- **采集**：矢量的厂牌标识也能归一成带边距的方图，仍然是矢量；白字标配深底，不会在浅底上看不见。
- **采集**：厂牌标识在圆形图位里摆得端正：自带大留白的裁掉多余空白，顶到边的补足四周，标识的四角
  不再被圆形切掉。像素不缩放，清晰度不变。
- **采集**：浅色笔画的位图标识同样配深底；自带整块底的标识保持原样。已装的标识重跑归一时从备份原图
  开始，算法的改进也能落到它们身上。
- **采集**：找厂牌标识时把「装着的那张太小、圆形图位得放大它才铺满」的厂牌也算进名单，站点上有更清晰
  的一份就换上去；找不到更大的只在复核件上留一行判词，装着的那张一个字节都不动。
- **采集**：比小圆片还小的厂牌标识，用自己的底色把画布补到圆片要的实像素，浏览器不再放大整张图。
  补出来的是这张图自己的底，笔画一个像素都不缩放，看着只是圆片里的标识变清楚了。
- **配置**：桌面快捷方式创建或移除失败时报出真实原因，而不是只说失败。
- **界面**：危险操作（移除媒体文件夹等）统一走同一个二次确认。
- **界面**：忙态提示会说明正在进行的动作；禁用的开关与按钮给出正确的颜色和光标。
- **关注**：登记来源时点「猜你喜欢」的推荐芯片能正常填进输入框。

## [0.27.1] - 2026-09-07

### 新增

- **播放**：离开详情页后视频缩到角落继续播，带快退、快进与播放器右键菜单。
- **复核**：候选支持多选与按组采用，看得到身份证据和本机文件，取值一致的补空候选直接落库。
- **馆藏**：扫描时识别并导入已有的 NFO 资料。
- **更新**：独立包可以在页面里下载并安装更新，显示进度、安装前备份数据库、失败回滚到旧版。
- **配置**：新增公共代理、开机自启与独立包卸载。
- **界面**：后台任务显示所在阶段与计数。

### 修复

- **采集**：检不出真人脸的头像不再自动安装。
- **更新**：更新过程出错时先关闭数据库连接再退出。
- **界面**：搜索下拉栏、设置分组、危险操作提示与多处空态按统一规范收齐。

## [0.16.0] - 2026-09-06

### 新增

- **配置**：可选的访问密码与设备会话期限，登录页可选择保持登录时间。
- **馆藏**：详情页五星评分，写进馆藏。
- **界面**：JAV 封面与缩略图各自可选官方封面或预览图。
- **界面**：侧栏顺序按日常动线重排。

### 修复

- **采集**：头像按检出的人脸取景，作品封面不再覆盖人像。
- **配置**：管理操作按网盘实际能力显示。

## [0.11.2] - 2026-09-06

### 新增

- **配置**：CloudDrive 来源引导、挂载映射与状态配置。
- **配置**：媒体文件夹可以一次声明多个。
- **采集**：高清封面采集与本机来源配置。
- **播放**：浏览器不兼容的格式按需转码后播放。
- **馆藏**：按事务所重新确认女优所属，结果写回实体。
- **界面**：站内滚动条改成覆盖式，整页与抽屉都不再占一列宽度。

### 修复

- **采集**：链接可达性探测扛住 TLS 抖动，一次握手失败不判定站点不可达。
- **界面**：首次设置页与配置页的控件、间距与视觉层次成批修正。

## [0.7.14] - 2026-09-05

首个 Windows 独立测试包。在它之前的开发（2026-08-14 起，含 macOS 菜单栏阶段）没有制品，
逐条变化见 Git 历史。

### 新增

- **桌面**：Windows 独立测试包与单机开箱配置，托盘首启拉起引导服务，首次运行页改成表单，`peach init` 提供终端问答。
- **馆藏**：事务所成为独立实体，厂牌与事务所各有索引页和资料页，标识自动采集。
- **账本**：备份自带保留规则，托盘启动时按规则清退。
- **配置**：绑定非回环地址却没有设置访问口令时拒绝启动。

### 修复

- **桌面**：独立包使用 UTF-8 启动日志。
- **配置**：首次设置后的跳转目标取新鲜读到的 mDNS 名。

[未发布]: https://github.com/longmeidao/peach/compare/v0.33.0...HEAD
[0.33.0]: https://github.com/longmeidao/peach/compare/v0.31.0...v0.33.0
[0.32.0]: https://github.com/longmeidao/peach/compare/v0.31.0...v0.32.0
[0.31.0]: https://github.com/longmeidao/peach/compare/v0.30.0...v0.31.0
[0.30.0]: https://github.com/longmeidao/peach/compare/v0.27.1...v0.30.0
[0.27.1]: https://github.com/longmeidao/peach/compare/v0.16.0...v0.27.1
[0.16.0]: https://github.com/longmeidao/peach/compare/v0.11.2...v0.16.0
[0.11.2]: https://github.com/longmeidao/peach/compare/v0.7.14...v0.11.2
[0.7.14]: https://github.com/longmeidao/peach/releases/tag/v0.7.14
