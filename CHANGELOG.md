# 变更日志

本文件记录 Peach 每个发布版本里值得使用者知道的变化。

格式遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[Semantic Versioning 2.0.0](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。分组标题是
「新增 / 变更 / 弃用 / 移除 / 修复 / 安全」，对应规范的 Added / Changed / Deprecated /
Removed / Fixed / Security。

每个条目前面带一个区域标签：分组标题按变化性质分，标签按使用者看到的区域分，
取值限于「界面 / 播放 / 馆藏 / 采集 / 复核 / 关注 / 配置 / 更新 / 桌面 / 账本」。

版本号只在发布点推进：每个 `X.Y.Z` 都对应一份可下载的制品，主线上的其它提交由 commit
标识（ADR-0012）。`## [未发布]` 收的是已经进了主线、还没发出去的变化。

本文件只收有制品的版本。`0.7.0`–`0.7.2` 早于发布工作流，`0.8`–`0.29` 区间里的版本号由
每次本地集成推进产生，两段都没有对应制品，逐条变化见 Git 历史。

条目草稿由 `python scripts/changelog.py` 从提交主题生成，措辞由人改成使用者读得懂的话；
只改开发过程的提交（文档、测试、重构、构建）不进本文件。

## [未发布]

### 修复

- **界面**：完成首次设置后，右下角会跨页面无闪烁地显示安装教程；六项任务按真实状态自动打勾，也可逐项跳过，全部处理后自动消失。
- **馆藏**：新导入会规范 NFO 标签，并为精确匹配的女优补别名与首次头像。

## [0.34.1] - 2026-09-20

### 修复

- **桌面**：独立测试包可选择局域网访问，其他设备通过配置的端口与 `.local` 地址打开。
- **馆藏**：扫描结束后立即落库符合安全规则的资料，其余候选继续留在复核。
- **馆藏**：NFO 与 120 MiB 以上视频不再误入垃圾队列，同番号的大体积短版本归入重复项。
- **配置**：首次设置按 BoardUI 统一分类层级、间距与按钮，访问密码改为开关，访问地址按范围显示。
- **桌面**：完全卸载清理设置备份与解压目录。

## [0.34.0] - 2026-09-19

### 新增

- **界面**：头像抬起、清空溶解、徽标弹出与标题揭示落到目录、搜索、读数与详情
- **界面**：原地换态的六档动效落到字形、读数、骨架、开关与成败反馈
- **复核**：人工复核整页归 React，判定只改缓存里那几行
- **馆藏**：清空文件夹时连文件已消失的账本行一起删
- **界面**：光晕铺到每一块玻璃，配色钮同时挑强调色
- **采集**：javbus 降为兜底来源，官方单独说话时直接替换现值
- **界面**：界面音效：按钮、开关、菜单、弹层与状态通知各配一声轻响，设置里可关
- **关注**：关注管理整页归 React，表格视图交给 TanStack Table
- **界面**：侧栏玻璃上那几团漂动的光改成自己配色的光晕
- **复核**：番号目录按片长确认，复核那一类改叫无法识别
- **复核**：只有一家社区来源也补空，社区来源不一时取 javdb
- **配置**：演示库画面改用人工复核过的图库人像
- **馆藏**：从复核候选里认出同一位出演者的另一个艺名
- **馆藏**：日英并写的厂牌名认得出是哪一家
- **界面**：首页光晕预设搬到侧栏圆钮，参数区改为自绘拉条与色板弹层
- **界面**：口味页整页归 React，分析范围写进键里、图表都是 React 组件
- **馆藏**：只出英文的来源也收进同一套中文标签
- **界面**：首页光晕改为参数化并开放设置面板自定义
- **界面**：统计页整页归 React，读数卡兼页签、环形库存图与排行都在 React 层
- **配置**：配置页与换头像整页归 React，前端只剩一档渲染层
- **采集**：扫描与采集的卡片和横幅改为 React 档
- **配置**：来源和凭证页改为 React 档
- **播放**：设置里加一颗「播放兼容修复」，后台批量修缺时间戳表的 MP4
- **采集**：高清版目标页迁到 React 档，换页时卸下管理区正文上的 island
- **界面**：React 子树共用一个 TanStack Query 客户端
- **播放**：缺 ctts 的 MP4 改为重建头无损播，不再实时转码
- **采集**：JavBus 作第三个社区来源，封面只有一个图源时照样装上
- **关注**：首次采集补足 30 条，查找先读 F95 名片并随登记写下别名
- **配置**：访问密码试点改用 React 与 BoardUI 原版组件
- **采集**：官方渠道落空时用 AVBase 与 javdb 补资料和封面，采集完成用通知报

### 变更

- **界面**：馆藏网格视口外的卡片跳过渲染

### 修复

- **界面**：修正封面取景与作品卡入口
- **界面**：补齐目录页的原地换态动效
- **播放**：全屏播放时点进度条右段不再关掉详情
- **界面**：配色钮留住调色盘字形，关光晕后浅色玻璃回到蓝灰，标题可点开
- **界面**：换头像弹层盖住整页，详情浮窗让出「接着看」，影院画面撑满
- **界面**：关掉光晕就收起那一组球，详情浮窗的滚动条归全站那一条
- **界面**：已迁 React 的八页外观对回迁移前的 Board 样式
- **馆藏**：判定文件已消失前逐条复核一次
- **配置**：打开设置时收掉还开着的锚定弹层
- **馆藏**：同一实体在一家来源只留一个外部 id，第二个丢掉而不是抛异常
- **馆藏**：长跑批处理不再领回收站里的行
- **馆藏**：番号目录的番号只补给正片
- **馆藏**：片长也按 javdb 优先取
- **复核**：厂牌与系列按账本别名落到规范名
- **配置**：演示人像下载用共用的浏览器 User-Agent
- **复核**：出演者按人比对，不按写法比对
- **馆藏**：企划名义不再挑战账本里的主艺名
- **复核**：复核队列的「账本现值」按账本此刻算
- **复核**：转售店口径和厂牌别名不再占复核队列
- **馆藏**：发行站自己交付的文件名也认得出番号
- **复核**：复核页自动落库提示与下面的队列留出行距
- **馆藏**：导入口味数据先换缓存再切范围，回到看过的范围读的是导入后的数
- **界面**：首页光晕只留光斑层并换成不含青绿的默认配色
- **采集**：演员标记不算内容标签，采集重新看得见它们的 genre
- **馆藏**：两边写法一样的 genre 不算停在英文
- **馆藏**：合集与淫语各归一个名字，停在英文的 genre 候选摘掉重抓
- **馆藏**：英文写法与日文原词同去向，词边界不再是退格符
- **馆藏**：genre 取日语原词，标签去重并改成通行译名
- **复核**：番号目录那一类复核队列改为现算，不再读 CSV 快照
- **馆藏**：站名包着番号的目录也认作发行目录，不是创作者
- **复核**：复核页报出自动落库结果，字段名徽章在亮色下看得见
- **馆藏**：前导零的卷号也认作分卷
- **播放**：VC-1 等编码走软件解码，分片开头不再出绿帧
- **馆藏**：来源说「没有」按告知项报一个数，不占问题清单
- **采集**：AVBase 取商品号认得出番号的那条，合集不再顶替本作资料
- **采集**：账本里当 code 用的目录名按无番号处理，不再报成问题项
- **播放**：修复头的后台任务取错了 ffprobe 的路径
- **采集**：既有库采集读对凭据根，采集设置里贴的 Cookie 才跟着走
- **采集**：联网来源只补账本没有值的字段，同值候选不进复核
- **播放**：有 B 帧却没有 ctts 的 MP4 改走转码
- **馆藏**：连号图集里和正片同名的那张不当本地海报
- **配置**：设置页保存键统一叫保存配置，操作键改用主按钮
- **采集**：封面失败原因分清下架占位图、单一图源与图源不一致
- **配置**：设置页折叠恢复高度动画，检查频率说明并进行内
- **配置**：设置弹层各组开关行坐上灰卡，卡内分块改白底
- **界面**：媒体库图标面板恢复七列网格，React 类名避开旧样式表
- **界面**：收起排名时展开键钉在原位，页面不再先上移再回滚
- **界面**：排名与侧栏名单展开改走 Collapse 的高度过渡，展开时看得见动画
- **界面**：统计与口味页的排名展开收起带上高度过渡
- **界面**：React 样式产物按 Tailwind 浏览器基线输出，CI 重建与提交一致
- **界面**：React 子树按 BoardUI 原值声明同名 token，跟随系统的深色文字色与手动深色一致
- **界面**：iOS 状态栏回到实色，筛选条上滚放出改走合成线程不再卡顿
- **界面**：窄屏顶栏改为 fixed，手机聚焦搜索框时页面不再被 Safari 往上滚
- **采集**：本地 NFO 给出的字段免复核补空，r18 资料取日文写法；候选卡片高亮盖住右侧圆点
- **界面**：侧栏遮罩不再给 iOS 状态栏变色，筛选框恢复全宽，接着看无内容时整块不出现
- **界面**：顶栏外壳对 iOS 状态栏取色不可见，滚动后状态栏仍透出内容
- **关注**：同一作者跨站的版本合成一张卡、列表按组分页；手机搜索键盘不再把顶栏推出屏幕
- **界面**：iOS 状态栏滚过吸顶筛选框、开合抽屉、打开详情后仍透出内容
- **关注**：同期分帖的版本按相似标题归组，回抓不再原地重放首页，名片头像补上 X 与 Patreon
- **界面**：收起的遮罩退出渲染树，iOS 状态栏与地址栏透出内容；卡片标题封顶两行
- **界面**：iOS 玻璃不再冲成 HDR 高亮，手机详情能滚、画面更大，接着看能横滑
- **关注**：作者、来源、题材筛选点什么就只看什么
- **关注**：归档站多附件帖进详情轮播，宽高只补图片卡面
- **关注**：详情原图取不到时退回缩略图，卡面比例连视频也按缩略图量
- **关注**：身份键按 Unicode 留字母数字，中日文名不再被抹成空串
- **关注**：存量行重抓的检查参数离开 older 时拒收，做法写进复用清单
- **关注**：按名字查找的结果收下添加框下拉给过的每个名字
- **关注**：整行重写保留清单里每张图学到的宽高
- **关注**：图片全隐藏的条目卡面不再挂着被隐藏的图
- **关注**：按名字查找 rule34.xxx 时前缀命中的标签也列为候选

## [0.33.0] - 2026-09-14

### 新增

- **关注**：fanbox 的关注可以从头全量回抓，正文里的媒体和卡面都补收封面图；Poll 帖不拿首位的图表当封面，媒体加载不出来时给出说明。
- **关注**：关注条目可以按单张图隐藏与恢复，不用整条删掉。
- **关注**：关注页顶上多一排题材：题材归成系列、配本库里最热的那张代表图，圆标按检出的人脸取景，每进一次页面换一批；本库挑不出够清楚的头像时去站点找。
- **关注**：「添加关注」边敲字边出建议，本机已有的和站上的标签分两组给；rule34.xxx 的作者单独分出一组，等站点时下拉里自报正在做事；F95 按标题约定和首楼名片认作者名与头像。
- **关注**：关注列表分默认视图与表格视图，表格有框线和可排序的列；作者与来源筛选可以同时选多个，行首各有「全选」「全不选」（多个标签仍按「同时具备」筛，所以标签行只有「全不选」）。
- **关注**：筛选多一档「未看时间范围」。
- **关注**：作者头像与来源图标由 Peach 取回保存在本机，页面不直接向对方站点要图；站点打不开时继续显示已保存的那张。视频与图片仍不保存。
- **关注**：创作者别名可以自己输入，首次采集的范围在同一张表单里设。
- **馆藏**：产地成为独立的一个维度（JAV 只是它的一种投影），筛选项、详情入口和批量判定都认它；韩国 MIB 不再算 JAV。
- **馆藏**：扫描和采集可以分开跑，各自有入口与进度；进度里列出当前项、失败项可以只重试这些。
- **馆藏**：外挂字幕落库并转成 WebVTT 挂进播放器。
- **馆藏**：番号解析认 Tokyo-Hot 与西片的日期写法。
- **馆藏**：广告包按形态证据进垃圾复核队列，高置信的那些可以批量入回收站。
- **播放**：视频按时间点预抽缩略图，进度条上就能看到那一刻的画面；抽帧密度在设置里选。
- **采集**：可以从 MIB 官网采集作品资料、封面、演员与厂牌标识。
- **采集**：javdb 采集同一趟产出中文别名候选，落库按批次署名、整批撕得回。
- **采集**：图库里只命中一张的女优头像批量装上，其余产一份对照表等人挑；头像上的站点水印会被检出并移除。
- **采集**：JAV 的横版封面按 2:3 取竖海报，大图卡片只显示书脊折痕右侧的正封，两侧用模糊背景垫。
- **采集**：没有番号的视频可以联网识别，也可以拿同目录的图片当本地海报。
- **采集**：鉴权失败单列一档，撞墙的来源这一批不再重复问。
- **采集**：抽取高清封面时页面上写着「正在抓取封面」；离开这一页再回来，还在跑的那一趟接着显示。
- **采集**：198 张厂牌标识随仓库分发，取不到图时回落到内置资源。
- **复核**：复核卡上可以就地收录未收录的 genre，候选改成单选卡片。
- **账本**：真相字段记字段级归属与写入序号，批准支持乐观并发：同一条被两头同时改时，后一次会被挡下而不是静默覆盖。
- **界面**：首页、索引页、标签字母表、资料页、关注页和数据管理都按 Board 重排；筛选条在首页与资料页共用一套，窄屏只钉住最左那一档，生效的筛选按 Chip 画，顶上几排横滚到头接着续下一批。
- **界面**：搜索栏边敲字边补出馆藏里的身份与作品。
- **界面**：详情点浮窗外面就退出，开场先挂本地封面占住海报位。
- **界面**：活动页把任务中心摊在一屏，后台任务、批处理和命令行跑的活儿都在里面。
- **界面**：资料页可以自己换头像；全站头像按检出的人脸取景。
- **界面**：没设访问密码时，卡片里常驻一条警示。
- **界面**：取不到的外链可以勾选重试；数据管理页的来源标官方站标，操作键统一成主按钮。
- **界面**：社媒标记取各家品牌色，常见七家走图标库。
- **配置**：设置里提供自动更新选项；新增「头像与站点图标刷新」，可选每周、每月、每季或从不，作者头像、来源图标和资料页外链圆标都按它重取。
- **更新**：GitHub Release 的正文取自本文件对应那一节，缺这一节就不发。

### 变更

- **界面**：整套界面换成 Board 风格：顶栏、侧栏和吸顶浮层是半透明玻璃，卡片、按钮、输入框和菜单按同一套尺寸、圆角与间距重画，明暗两个主题各成一套。设置里的「增加对比度」关掉玻璃的透明与折射，改用实色背景。
- **配置**：配置页搬进右上角的设置里，成为「这台电脑」这一块，按「通用 / 媒体 / 网络与访问 / 更新与维护」分开列，管理菜单不再单列「配置」。在别的设备上打开设置仍看得到这一块，里面写明这几项要在运行 Peach 的那台电脑上改。
- **播放**：分片重编码走 NVENC，同一分片只起一个 FFmpeg；不兼容的片源即使账本没有时长也按片切，不整片转码。
- **采集**：来源说「没有」的番号 7 天内不再问，没有番号的视频不列成问题项；FC2 的番号只问封面，资料不再向 r18.dev 要。
- **馆藏**：扫描按目录列表自带的大小与时间登记文件，只采集跳过齐全的那些行。
- **关注**：站点不通时，联想和圆标补全立刻回空，不担超时的等待。
- **配置**：开机启动项的状态按快捷方式指纹留存，服务起来时就预热好，点开设置不用等。

### 修复

- **界面**：女优、厂牌等资料页顶上那块筛选浮层改成和首页同一块玻璃——标签、计数和排序键的大小、间距、圆角全部对齐，标签一行滚到头时两端渐隐，不吸顶时也不再显出上下两层。
- **界面**：玻璃、骰架与浮层收归一套：滚动时按落定后的布局算位置，选中那块按跳的跳幅决定冲过头多少，骰架按页面结构画并跟着容器走，浮层上下留一样宽的空隙。
- **界面**：窄屏上搜索开合的聚焦不带着页面滚，筛选框按滚动方向切换吸顶，资料页那排外链改横滚；会横向溢出的层一律配滚轮映射与两端渐隐。
- **界面**：照片墙改等宽网格、各列从顶上开始排，关注图片墙的瀑布流不随图片加载重排。
- **界面**：厂牌标识摆进圆形图位不发空也不被切角，比小圆片还小的用自己的底色补到 64 实像素。
- **界面**：数据管理页和关注页上几处说明改成读得懂的话——链接按「官网/事务所、社交账号、作品资料站、资料出处」分类，检查结果分成「页面已经没了」和「这次没访问成功」两组并写明区别，文件检查说清是拿馆藏里的记录去磁盘和网盘上找文件，关注凭据那一段写明它存成运行 Peach 那台电脑上的一个文件、谁能打开它。
- **复核**：复核页顶上的工具条和分组条从进页面起就是玻璃，两条合成一块，左右和标题、卡片同一条边；上面那排页签也回到同一条左边。
- **复核**：候选落库前核验来源自报的番号。
- **播放**：人脸检出跳三个尺度各检一次、分数取中位数，圆标不再框在躯干上；客户端断开后 Range 响应停止读文件。
- **关注**：圆标取景按人脸：脸不够清楚就摆全身，落盘前裁掉源图自留的黑边，小圆标里的脸按像素下限放大。
- **关注**：查找进度按计划的来源数走满，手柄型 id 不分大小写；合辑按同一份画面作者计数。
- **采集**：书脊折痕按正封形状认，切点走到斜坡尽头，正封左缘不再压着书脊；韩国 MIB 不向 JAV 来源要封面。
- **采集**：番号闸门保住 Tokyo-Hot 的小写写法，也认日期式的两种分隔符。
- **采集**：只凭状态码判出的 403 不替你断定成因，采集检查的裸 403 不再要求去登录；Gfriends 索引退回旧缓存时告警，未收录不记成结论。
- **采集**：扫描与采集的死任务状态写回中断，界面不再来回跳。
- **馆藏**：同一番号下文件名一样的两份算同一条重复；日期式番号的分隔符进身份，不再折叠。
- **账本**：清退账本备份时顺手扫掉无主的 `-wal`/`-shm`。

## [0.31.0] - 2026-09-08

### 新增

- **关注**：SimpCity 线程可以作为关注来源。站点不让游客读帖，需要把你自己的登录 Cookie 写进凭据文件；Peach 不解任何机器人质询。
- **关注**：在「添加关注」里输入名字查找时，也会用你的 Cookie 搜 SimpCity 站内线程标题，结果带上版块标签（如 OnlyFans、Simp Chat）供你分辨。
- **关注**：来源筛选菜单新增「全选」与「全不选」。

### 修复

- **关注**：SimpCity 帖子里的网盘链接（Gofile、Pixeldrain 等）现在能从站点的跳转链接里还原出来，详情页会显示对应按钮；帖子里的图片不论挂在站方图床还是哪家第三方图站，都能在详情页就地查看（Peach 代取时不带任何登录态，也不会去取内网地址）；有图的楼层以图为主，网盘链接只做按钮；来源图标补上 SimpCity。

## [0.30.0] - 2026-09-08

### 新增

- **配置**：新增桌面快捷方式开关，可直接创建或移除桌面图标。
- **界面**：可折叠的区块标题统一带上展开箭头。

### 移除

- **关注**：命令行的 `peach follow` 子命令删除；登记、检查、复核与保存全部在关注管理页完成。

### 修复

- **采集**：矢量的厂牌标识也能归一成带边距的方图，仍然是矢量；白字标配深底，不会在浅底上看不见。
- **采集**：厂牌标识在圆形图位里摆得端正：自带大留白的裁掉多余空白，顶到边的补足四周，标识的四角
  不再被圆形切掉。像素不缩放，清晰度不变。
- **采集**：浅色笔画的位图标识同样配深底；自带整块底的标识保持原样。已装的标识重跑归一时从备份原图
  开始，算法的改进也能落到它们身上。
- **采集**：找厂牌标识时把「装着的那张太小、圆形图位得放大它才铺满」的厂牌也算进名单，站点上有更清晰
  的一份就换上去；找不到更大的只在复核件上留一行判词，装着的那张一个字节都不动。
- **采集**：比小圆片还小的厂牌标识，用自己的底色把画布补到圆片要的实像素，浏览器不再放大整张图。
  补出来的是这张图自己的底，笔画一个像素都不缩放，看着只是圆片里的标识变清楚了。
- **配置**：桌面快捷方式创建或移除失败时报出真实原因，而不是只说失败。
- **界面**：危险操作（移除媒体文件夹等）统一走同一个二次确认。
- **界面**：忙态提示会说明正在进行的动作；禁用的开关与按钮给出正确的颜色和光标。
- **关注**：登记来源时点「猜你喜欢」的推荐芯片能正常填进输入框。

## [0.27.1] - 2026-09-07

### 新增

- **播放**：离开详情页后视频缩到角落继续播，带快退、快进与播放器右键菜单。
- **复核**：候选支持多选与按组采用，看得到身份证据和本机文件，取值一致的补空候选直接落库。
- **馆藏**：扫描时识别并导入已有的 NFO 资料。
- **更新**：独立包可以在页面里下载并安装更新，显示进度、安装前备份数据库、失败回滚到旧版。
- **配置**：新增公共代理、开机自启与独立包卸载。
- **界面**：后台任务显示所在阶段与计数。

### 修复

- **采集**：检不出真人脸的头像不再自动安装。
- **更新**：更新过程出错时先关闭数据库连接再退出。
- **界面**：搜索下拉栏、设置分组、危险操作提示与多处空态按统一规范收齐。

## [0.16.0] - 2026-09-06

### 新增

- **配置**：可选的访问密码与设备会话期限，登录页可选择保持登录时间。
- **馆藏**：详情页五星评分，写进馆藏。
- **界面**：JAV 封面与缩略图各自可选官方封面或预览图。
- **界面**：侧栏顺序按日常动线重排。

### 修复

- **采集**：头像按检出的人脸取景，作品封面不再覆盖人像。
- **配置**：管理操作按网盘实际能力显示。

## [0.11.2] - 2026-09-06

### 新增

- **配置**：CloudDrive 来源引导、挂载映射与状态配置。
- **配置**：媒体文件夹可以一次声明多个。
- **采集**：高清封面采集与本机来源配置。
- **播放**：浏览器不兼容的格式按需转码后播放。
- **馆藏**：按事务所重新确认女优所属，结果写回实体。
- **界面**：站内滚动条改成覆盖式，整页与抽屉都不再占一列宽度。

### 修复

- **采集**：链接可达性探测扛住 TLS 抖动，一次握手失败不判定站点不可达。
- **界面**：首次设置页与配置页的控件、间距与视觉层次成批修正。

## [0.7.14] - 2026-09-05

首个 Windows 独立测试包。在它之前的开发（2026-08-14 起，含 macOS 菜单栏阶段）没有制品，
逐条变化见 Git 历史。

### 新增

- **桌面**：Windows 独立测试包与单机开箱配置，托盘首启拉起引导服务，首次运行页改成表单，`peach init` 提供终端问答。
- **馆藏**：事务所成为独立实体，厂牌与事务所各有索引页和资料页，标识自动采集。
- **账本**：备份自带保留规则，托盘启动时按规则清退。
- **配置**：绑定非回环地址却没有设置访问口令时拒绝启动。

### 修复

- **桌面**：独立包使用 UTF-8 启动日志。
- **配置**：首次设置后的跳转目标取新鲜读到的 mDNS 名。

[未发布]: https://github.com/longmeidao/peach/compare/v0.34.1...HEAD
[0.34.1]: https://github.com/longmeidao/peach/compare/v0.34.0...v0.34.1
[0.34.0]: https://github.com/longmeidao/peach/compare/v0.33.0...v0.34.0
[0.33.0]: https://github.com/longmeidao/peach/compare/v0.31.0...v0.33.0
[0.31.0]: https://github.com/longmeidao/peach/compare/v0.30.0...v0.31.0
[0.30.0]: https://github.com/longmeidao/peach/compare/v0.27.1...v0.30.0
[0.27.1]: https://github.com/longmeidao/peach/compare/v0.16.0...v0.27.1
[0.16.0]: https://github.com/longmeidao/peach/compare/v0.11.2...v0.16.0
[0.11.2]: https://github.com/longmeidao/peach/compare/v0.7.14...v0.11.2
[0.7.14]: https://github.com/longmeidao/peach/releases/tag/v0.7.14
