/* 标签显示名。纯查表，没有 DOM 也没有请求，所以从 app.js 拆出来单独放。

   拆出来的理由有两条。一是它在 app.js 里被定义在使用点之下：卡片渲染（靠上）
   调 `tagLabel()`，而声明在四千行之后，`const` 的 TDZ 让这种排布只在「调用发生
   在启动之后」时才成立。二是它是本文件里最容易独立验证的一类逻辑，放进模块后
   将来能被单元测试直接 import，不必再从页面源里找字符串。

   映射只做「显示名」，不做同义词归并：账本里的标签名是真相，这里只负责把几个
   写法别扭的显示成人看得懂的样子。查不到就原样返回。 */
const TAG_DISPLAY_NAMES={'1080P':'1080p','60fps':'60FPS','AI去码':'AI解码',
  '淫语ASMR':'ASMR','JK制服':'JK','OL制服':'OL','眼镜':'眼镜娘','情趣内衣':'性感内衣',
  '口罩遮脸':'口罩','强制剧情':'强制','足系':'美腿','足交':'脚交','骑乘':'骑乘位',
  '后入':'背后位','3P多人':'3P','双洞齐插':'双洞齐下','毒龙':'毒龙钻'};
const tagLabel=tag=>TAG_DISPLAY_NAMES[tag]||tag;

export {TAG_DISPLAY_NAMES, tagLabel};
